# Free-Script 全代码审查问题大全 + 修复方案大全（2026-06-28 全量复审）

> **审查范围**：项目全部 JS（~5.3 万行）+ CSS（~1.4 万行）+ HTML（~2.8 千行），共 18 个 JS 文件 / 5 个 CSS 文件 / 1 个 HTML 文件
> **审查方法**：5 个并行子代理逐行读取 + Grep 全目录交叉验证 + 跨文件重复模式核对
> **基线对比**：相对 06-28-最终版报告，本轮发现项目已大幅重构（`modules.js` / `patch.js` 已拆分删除，新增 `js/modules/`、`js/ai-contract/`、`js/state/` 目录），但仍有 **15 项 P0 + 33 项 P1 + 48 项 P2 + 32 项 P3 = 共 128 项** 待修问题。
> **本次审查未修改任何文件**，所有结论均经 Grep 在 `/workspace/js` 与 `/workspace/index.html` 范围内交叉验证。

---

## 第 0 章：执行摘要

### 0.1 问题全景统计

| 维度 | 本轮待修 | 主要类型 |
|------|---------|---------|
| **P0 严重** | **15** | 双重注册 / 跨文件裸全局 / 双写 / 缓存失效 / XSS / 撤销 bug / CSP |
| **P1 高** | **33** | 死代码簇 / 上帝函数 / 跨文件重复 / 双写链 / 性能反模式 |
| **P2 中** | **48** | 重复代码 / 硬编码 / a11y / 性能 / 内联样式 / 死注释 |
| **P3 低** | **32** | 风格 / 命名 / 微优化 / 失实注释 |
| **合计** | **128** | — |

### 0.2 核心结论（三句话）

1. **架构层双写仍未根治**：`mergeRelationships` + `_pushRelationshipsToGM` 跨 3 文件双写链（systems.js:865 + ai-response-mutator.js:627 + phone-ui.js:5066）仍在运行；货币、keyEvents、worldSnapshot 在 AIResponseMutator 已写入后仍被 game.js 二次写入；phone-ui.js 仍有 54 处直写 `gameState.* =`。
2. **死代码清理有进展但未完成**：6 组函数（时间格式化 5 个、`_getCachedRegExp`、`isSaveLocked`、`extractObj`、`toggleBook` 等）已加注释标记但函数体仍残留；正则缓存 `_getCachedRegExp` 已写好却从未被 extract* 接入。
3. **正确性 bug 集中爆发**：撤销快照写入不存在的 `transient` 域导致对话历史无法恢复（P0-13）、`appendForumReply` 把时间戳数字当字符串显示（P0-11）、moments 朋友圈点赞/评论按钮无响应（P0-10）、装饰标签 `danmu`/`danbu` 拼写分歧导致弹幕泄漏（P0-6）。

---

## 第 1 章：P0 严重问题（15 项）—— 立即修复

### P0-1 双重注册全局错误处理器，每次报错触发两次 toast / 两次 console.error
- **文件**：[utils.js:439-440](file:///workspace/js/utils.js#L439-L440) + [init.js:13-30](file:///workspace/js/init.js#L13-L30)
- **类型**：重复注册 / 正确性
- **描述**：两处都通过 `GlobalCleanup.registerListener(window, 'error', ...)` 注册了全局错误处理器。`utils.js` 版本仅 console.error；`init.js` 版本含资源错误过滤 + UI.toast。两者都被触发，每次未捕获错误产生 2 条 console.error + 最多 2 个 toast。
- **修复**：删除 `utils.js:439-440`，保留功能更完整的 `init.js` 版本。
- **证据**：`grep "registerListener(window, 'error'"` 命中 utils.js:439 + init.js:13。

### P0-2 双重注册 document click 事件委托，toggle-thought 失效
- **文件**：[utils.js:62-104](file:///workspace/js/utils.js#L62-L104) + [init.js:344-370](file:///workspace/js/init.js#L344-L370)
- **类型**：重复注册 / 正确性
- **描述**：两套机制都监听 `document` 的 click 事件并按 `data-action` 路由。`utils.js` 用 `window[action](...args)` 调任意全局函数；`init.js` 用 `actionHandlers` 映射表（仅 `toggle-thought`）。对 `data-action="toggle-thought"` 元素，两个处理器都触发，`toggleThought` 被调用两次，状态翻回原值，**功能失效**。
- **修复**：删除 `init.js` 的 `_setupGlobalEventDelegation`，把 `toggle-thought` 合并进 `utils.js` 的 `_globalA11yDelegate` 路由表（或加白名单避免重复触发）。
- **证据**：`grep "registerListener(document, 'click'"` 命中 utils.js:103 + init.js:354。

### P0-3 跨文件裸全局变量写入无 typeof 守卫，潜在 ReferenceError
- **文件**：[game.js:2757-2758](file:///workspace/js/game.js#L2757-L2758) 声明 + [core.js:1999-2000](file:///workspace/js/core.js#L1999-L2000) + [phone-ui.js:3891-3892](file:///workspace/js/phone-ui.js#L3891-L3892) 写入
- **类型**：正确性 / 架构
- **描述**：`_streamModeLocked` / `_streamMode` 在 `game.js` 用 `var` 声明为全局，但 `core.js` 和 `phone-ui.js` 写入时无 `typeof` 守卫。唯独 `_streamFullText` 用了 `if (typeof _streamFullText !== 'undefined')` 守卫。若 `resetRuntimeState` 在 `game.js` 加载前被执行，会抛 `ReferenceError: _streamModeLocked is not defined`，阻断 reset 流程。
- **修复**：将三个变量收拢到 `RuntimeState` 或新建 `StreamState` 命名空间；或在 core.js/phone-ui.js 两处补齐 `typeof` 守卫。
- **证据**：`grep "_streamModeLocked"` 命中 game.js:2757/928 + core.js:1999 + phone-ui.js:3891；守卫策略不一致。

### P0-4 货币字段在 AIResponseMutator + game.js 双写
- **文件**：[game.js:1907-1937](file:///workspace/js/game.js#L1907-L1937)
- **类型**：双写 / 正确性
- **描述**：AIResponseMutator.apply() 已通过 `_applyCurrency` 写入 `StateManager.set('entities.currency')`。但 game.js:1907-1937 的货币处理块**未受 `_doLegacyStateWrites` 守卫**，在 `_aiMutatorApplied=true` 时仍再次执行 `StateManager.set('entities.currency', ...)`，并额外触发 `CurrencyReconciler.reconcileFromStory`（从 storyText 提取金额后用 REPLACE 语义覆盖 mutator 写入的值）。
- **修复**：用 `if (_doLegacyStateWrites && ...)` 守卫整个货币块；reconcileFromStory 仅在 mutator 未应用时作为兜底。
- **证据**：ai-response-mutator.js:268-279 写 entities.currency；game.js:1919-1933 再次写 + reconcile 覆盖。

### P0-5 keyEvents 双写（mutator + game.js）
- **文件**：[game.js:1939-1953](file:///workspace/js/game.js#L1939-L1953)
- **类型**：双写 / 正确性
- **描述**：AIResponseMutator._applyKeyEvents 已把 data.keyEvents 写入 gm.events / StateManager.entities.events。game.js:1939-1953 又通过 `_gm.addImportantEvents(_keyEventObjs)` 再写一遍，**未受 `_doLegacyStateWrites` 守卫**。同一批事件被 import 两次，gm.events 中重复条目 + 重复订阅通知。
- **修复**：加 `if (_doLegacyStateWrites && ...)` 守卫。
- **证据**：ai-response-mutator.js:61 steps 含 keyEvents；game.js:1944-1950 无守卫再调 addImportantEvents。

### P0-6 装饰标签 `danmu` / `danbu` 拼写不一致，弹幕泄漏显示给玩家
- **文件**：[game.js:1376](file:///workspace/js/game.js#L1376) vs [game.js:2885](file:///workspace/js/game.js#L2885)
- **类型**：正确性
- **描述**：sendAIRequest 内联正则（1376）用 `danmu`，模块级 `_reDecorTagsTyping`（2885）用 `danbu`。两者本应匹配同一装饰标签，但拼写不同 → 流式 tick 期间用 `_reDecorTagsTyping` 清不掉 `danmu` 标签，最终渲染用 1376 的正则又清不掉 `danbu` 标签。AI 输出的弹幕标签会泄漏显示给玩家。
- **修复**：统一为同一拼写（确认实际标签名后全量替换）。
- **证据**：`grep "danmu|danbu"` 仅这两处，拼写分歧。

### P0-7 MemoryManagerUI 各 save* 函数未失效 longTermMemory 缓存
- **文件**：[tavern-compat.js:4534-4561](file:///workspace/js/tavern-compat.js#L4534-L4561)（saveItem）、4613-4652（saveNewItem）、4693-4744（saveLocation）、4770-4805（saveNewLocation）、4450-4484（saveCharacter）
- **类型**：正确性
- **描述**：这些函数都先调 Mutator（BagMutator.setItems / CharacterMutator.replaceCharacter / StateManager.set('entities.locations')）写入权威源，成功后再手动改 `gm.tables.items[name]` / `gm.tables.characters[name]` / `gm.tables.locations[name]` 补齐运行时字段。但**没有任何一处显式置 `gm._ltmDirty = true`**。`longTermMemory` getter 有缓存机制：`if (self._ltmCache && !self._ltmDirty) return self._ltmCache`。结果：用户在 MemoryManagerUI 编辑角色/物品/地点后，下一次 `buildInjection` 读取 `longTermMemory` 仍命中旧缓存，AI 注入的"角色近况/物品变化/地点信息"是编辑前的数据，**UI 与 AI 上下文不一致**。
- **修复**：在每个 save* 函数末尾追加 `gm._ltmDirty = true;`；或封装 `gm._markLtmDirty()` 方法统一入口；或让 MemoryManagerUI 完全改用 GameMemory API（`recordCharacterChange` / `recordItemObtained` 等）。
- **证据**：tavern-compat.js:3710-3754 缓存定义；4552-4561 saveItem 改 `gmItemEntry.qty/unit/rarity/...` 无 `_ltmDirty` 设置；Grep 确认 `_ltmDirty = true` 设置点共 18 处，但 MemoryManagerUI 各 save* 函数均不在内。

### P0-8 `_autoExtractWorldNotes` 直接 push `longTermMemory.worldNotes`，与"只读快照"语义矛盾
- **文件**：[phone-ui.js:903](file:///workspace/js/phone-ui.js#L903) + [tavern-compat.js:3720-3744](file:///workspace/js/tavern-compat.js#L3720-L3744)
- **类型**：正确性 / 架构
- **描述**：`longTermMemory` getter 注释明确声明"`characterTable/itemTable/locationTable/relationships` 改为深拷贝快照，禁止外部通过引用直接写入"。但 `worldNotes` 字段却"返回引用以保留 push 语义"。`phone-ui.js:903` 因此直接 `EnhancedMemory.longTermMemory.worldNotes.push({...})`。这种字段间语义不一致极易误导维护者：看到 `worldNotes` 可 push，会以为其他字段也可 push，结果写入深拷贝快照后**静默丢失**。
- **修复**：统一为只读快照，提供 `GameMemory.addWorldNote(note)` API 替代直 push，内部置 `_ltmDirty=true`。
- **证据**：tavern-compat.js:3720-3744 注释与代码并存，`worldNotes` 是唯一返回引用的字段；phone-ui.js:903 直 push；phone-ui.js:4797-4798 也走同一 push 路径。

### P0-9 MemoryManagerUI 仍大量直写 `gameState` 绕过 StateManager
- **文件**：[tavern-compat.js:372](file:///workspace/js/tavern-compat.js#L372)、397、818、823、3985、3989、4450、4571、4594、4660、4846
- **类型**：架构 / 正确性
- **描述**：按项目架构，StateManager 是权威源，`_syncLegacyMirror` 单向镜像到 `gameState` 旧字段供 UI 只读兼容。但 MemoryManagerUI（本应只调 GameMemory API + Mutator）仍直写 `gameState`：行 372 `gameState.playerPersonality = argsStr`、行 397 `gameState.conversationHistory = []`（清空镜像数组但 StateManager 内部值陈旧）、行 4450 `gameState.allCharacters = {}`、行 4571/4594/4660 `gameState.currentBag`、行 4846 `gameState.rollingSummary`。直写绕过 `_syncLegacyMirror`，`StateManager.get` 返回陈旧值。
- **修复**：MemoryManagerUI 严格只调 GameMemory API + Mutator；TavernHelperCompat 的 `persona`/`clear`/`sys` 命令改走 `StateManager.set`；删除 saveItem/saveCharacter 等的 legacy 兜底分支。
- **证据**：Grep `gameState\.\w+ =` 在 tavern-compat.js 命中 11 处写入点。

### P0-10 `renderMomentsPage` 输出 `data-action` 但事件委托不处理 moments
- **文件**：[phone-ui.js:1908](file:///workspace/js/phone-ui.js#L1908)、1912 + `_applyLogPageStyle`（约 1444-1561）
- **类型**：正确性 / 功能性 bug
- **描述**：`renderMomentsPage` 行 1908/1912 输出 `data-action="toggleMomentLike" data-args='[idx]'` 与 `data-action="sendMomentComment"`，按项目 CSP 兼容策略应走事件委托。但 `_applyLogPageStyle` 仅处理 chat 的 data-action 委托，不处理 moments。结果：**moments 朋友圈的点赞、评论按钮点击无响应**（Grep 未发现其他全局委托逻辑覆盖）。
- **修复**：在 `_applyLogPageStyle` 或统一的事件委托入口补 moments 的 data-action 分支。
- **证据**：`grep 'data-action="toggleMomentLike"'` 仅在渲染输出中命中，未在委托处理中命中。

### P0-11 `appendForumReply` 把时间戳数字当字符串显示
- **文件**：[phone-ui.js:276](file:///workspace/js/phone-ui.js#L276)（存） + [phone-ui.js:320](file:///workspace/js/phone-ui.js#L320)（显示）
- **类型**：正确性 / 用户可见 bug
- **描述**：`sendForumComment`（行 276）、`requestForumNpcReplies`（行 414/420）都已按 P2-3 修复存 `time: Date.now()`（时间戳数字）。但 `appendForumReply`（行 320）显示时用 `escapeHtml(comment.time || '刚刚')` —— 时间戳数字（如 `1772345678901`）被当字符串直接渲染，用户看到的是一长串数字而非"刚刚"或"5分钟前"。NPC 回复（行 417-422 push 的时间戳）同样被错误显示。
- **修复**：显示时调用 `_formatLogTime(comment.time, 'time')` 或相对时间函数。
- **证据**：phone-ui.js:276 存 `time: Date.now()`；320 显示 `escapeHtml(comment.time || '刚刚')` —— `comment.time` 是数字，`||` 短路不触发。

### P0-12 phone-ui.js 整文件绕过 StateManager 直读写 legacy gameState（54 处）
- **文件**：[phone-ui.js](file:///workspace/js/phone-ui.js) 全文（文件头 6-33 行注释自认 40+ 处，实际 Grep 命中 54 处）
- **类型**：架构 / 正确性
- **描述**：文件头注释自己承认："phone-ui.js 大量函数仍直接读写 legacy 字段"。具体新发现：行 570 `gameState._currentDiaryNpc = name` 直写、行 585-591 `gameState._diaryDateOffset` 直写、行 6555-6562 `gameState._notifSeenSnapshot.chat[name]` 直写、行 7059-7060 `gameState._chatLogs[npcName]` 直写、行 7160-7166 `gameState.conversationHistory.push(...)` 直写（saveNpcEdit 注入，绕过 StateManager 导致注入消息不进入撤销历史）。直写绕过 `_syncLegacyMirror`，StateManager.get 返回陈旧值。
- **修复**：按文件头注释的"写入替换映射"逐项迁移到 Mutator；长期删除 `_syncLegacyMirror` 与 `gameState` 旧字段。
- **证据**：`grep "gameState\.\w+ ="` 在 phone-ui.js 命中 54 处；phone-ui.js:6-33 注释自认。

### P0-13 撤销快照写入不存在的 `transient` 域，对话历史无法恢复
- **文件**：[undo-mutator.js:69](file:///workspace/js/state/mutators/undo-mutator.js#L69)
- **类型**：正确性 / 数据不一致
- **描述**：`restoreFromSnapshot` 调用 `StateManager.set('transient.conversationHistory', ...)`，但 `StateSchema.getDefaultState()` 中**不存在 `transient` 域**，`_legacyToPath` 映射表中 `conversationHistory` 的正确路径是 `progress.conversationHistory`（schema.js:125）。写入 `transient.conversationHistory` 会在 state 树上创建一个孤立的 `transient` 子对象，既不会被 `_syncLegacyMirror` 同步，也不会被任何订阅者监听到。**这是导致撤销功能对话历史不恢复的直接原因**。
- **修复**：将 `'transient.conversationHistory'` 改为 `'progress.conversationHistory'`。
- **证据**：schema.js:35 `conversationHistory: []` 位于 `progress` 域；schema.js:125 `'conversationHistory': 'progress.conversationHistory'`；Grep `transient\.` 全项目仅此一处写入。

### P0-14 stscript-bridge loadPreset hook 引用未声明变量（条件性 ReferenceError）
- **文件**：[stscript-bridge.js:37-38](file:///workspace/js/ai-contract/stscript-bridge.js#L37-L38) 使用 + [stscript-bridge.js:67-68](file:///workspace/js/ai-contract/stscript-bridge.js#L67-L68) 声明
- **类型**：正确性
- **描述**：loadPreset hook 内执行 `_presetVarCacheKey = null; _presetVarParsed = false;`，但这两个变量在 line 67-68 的 `if (typeof injectPresetGlobalVars === 'function')` 块内才用 `var` 声明。由于 `var` 提升到函数顶部，运行时不会抛 ReferenceError，但若 `injectPresetGlobalVars` 不存在（hook 3 未建立），`_presetVarCacheKey` 永远是 `undefined`，loadPreset 的清空操作无实际效果。
- **修复**：将 `var _presetVarCacheKey = null; var _presetVarParsed = false;` 提升到 `initSTscriptBridge` 函数顶部，与 `_initRetryCount` 同级声明。
- **证据**：stscript-bridge.js:37 使用 vs :67 声明，相隔 30 行且在不同 if 块内。

### P0-15 CSP 允许 `unsafe-eval` 脚本执行
- **文件**：[index.html:9](file:///workspace/index.html#L9)
- **类型**：安全 / HTML
- **描述**：`Content-Security-Policy` 中 `script-src 'self' 'unsafe-eval'` 允许 `eval()`、`new Function()`，是 XSS 风险放大的高危配置。若任一引入脚本存在注入点，攻击者可执行任意代码。
- **修复**：排查 JS 中是否真的依赖 `eval`/`Function` 构造器（如动态模板编译），若仅历史遗留则移除 `'unsafe-eval'`；若必须保留，应在注释中写明依赖来源。
- **证据**：`<meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-eval'; ...">`

---

## 第 2 章：P1 高优先级（33 项）—— 架构债务 / 死代码

### 2.1 死代码簇（已注释标注但函数体残留，应删除）

| 编号 | 文件:行号 | 死代码 | 验证方式 |
|------|----------|--------|---------|
| P1-1 | [utils.js:562-604](file:///workspace/js/utils.js#L562-L604) | `_coerceTs` / `formatTime` / `formatDate` / `formatDateTime` / `formatTimeShort` / `migrateTimeFields` 6 个时间函数 | Grep 全项目零外部调用（core.js:2450 的 `GameTimeSystem.formatTime()` 是对象方法，无关） |
| P1-2 | [core.js:2585-2594](file:///workspace/js/core.js#L2585-L2594) | `_getCachedRegExp` + `_regexCache` Map | Grep 零调用；讽刺的是 extract* 每次都 new RegExp（见 P2-6） |
| P1-3 | [core.js:4410](file:///workspace/js/core.js#L4410)、[core.js:4413](file:///workspace/js/core.js#L4413) | `isSaveLocked` / `getSaveLockHolder` | Grep 仅命中定义行 |
| P1-4 | [core.js:2676](file:///workspace/js/core.js#L2676) | `extractObj` | Grep 仅命中定义；core.js:2716 注释失实（声称"仍被 game.js 多处用于"，实际 game.js 零调用） |
| P1-5 | [utils.js:522](file:///workspace/js/utils.js#L522) | `Logger.LEVELS` 对外暴露 | Grep 全项目无 `Logger.LEVELS` 读取 |
| P1-6 | [game.js:900-918](file:///workspace/js/game.js#L900-L918) | `applyParamPreset` | Grep `applyParamPreset\(` 全工作区仅命中定义本身 |
| P1-7 | [game.js:3387-3400](file:///workspace/js/game.js#L3387-L3400) | `deleteCharacter` | Grep 在 js/ 和 index.html 中均无调用（仅 backup/index.html 有旧版） |
| P1-8 | [phone-ui.js:3746](file:///workspace/js/phone-ui.js#L3746)、[phone-ui.js:3760](file:///workspace/js/phone-ui.js#L3760) | `showPresetSaveList` 链式双重 catch | Promise 语义：第一个 catch 返回 resolved promise，第二个 catch 永远不执行 |
| P1-9 | [systems.js:164](file:///workspace/js/systems.js#L164)、[systems.js:166](file:///workspace/js/systems.js#L166) | `_customTypes` / `_customStatuses` 的 for-in 循环 | 第 14 行注释明确"删除动态类型注册系统"，但 164/166 仍 for-in 这两个从未定义的属性 |
| P1-10 | [prompt-builder.js:23](file:///workspace/js/ai-contract/prompt-builder.js#L23)、[prompt-builder.js:30](file:///workspace/js/ai-contract/prompt-builder.js#L30) | `unregisterSection` / `getMode` 注释占位 | Grep 全项目零调用 |
| P1-11 | [smart-config-engine.js:309](file:///workspace/js/modules/smart-config-engine.js#L309) | `getConfigSummary` 注释占位 | Grep 全项目零调用 |
| P1-12 | [preset-manager.js:1368](file:///workspace/js/modules/preset-manager.js#L1368)、[preset-manager.js:1991](file:///workspace/js/modules/preset-manager.js#L1991) | `switchEntryState` / `buildBaseGameRules` 注释占位 | Grep 全项目零调用 |
| P1-13 | [macro-engine.js:22](file:///workspace/js/modules/macro-engine.js#L22)、[macro-engine.js:1254](file:///workspace/js/modules/macro-engine.js#L1254) | `saveLocalVars` / `saveGlobalVars` / `resetLocalVars` / `_dedent` 注释占位 | Grep 全项目零调用 |
| P1-14 | [regex-manager.js:874](file:///workspace/js/modules/regex-manager.js#L874) | `toggleScript` / `quickDeleteScript` 注释占位 | Grep 全项目零调用（注意 `_toggleScriptInGroup` 仍在用，不要混淆） |
| P1-15 | [worldinfo.js:626](file:///workspace/js/worldinfo.js#L626)、[worldinfo.js:2095](file:///workspace/js/worldinfo.js#L2095) | `toggleBook` / `toggleEntry` / `getInjectionByRole` 注释占位 | Grep 全项目零调用 |
| P1-16 | [game.js:3312-3316](file:///workspace/js/game.js#L3312-L3316) + [game.js:3345-3352](file:///workspace/js/game.js#L3345-L3352) | `fillChoiceToInput` 仅从不可达 else 分支调用 | `sendAIRequest` 永为 function，else 分支不可达 |
| P1-17 | [game.js:4319-4321](file:///workspace/js/game.js#L4319-L4321) | `ensureLogFallbacks` 末尾 `else if (typeof gameState !== 'undefined')` 直写兜底 | StateManager 由 index.html 静态加载，运行时必存在，else 不可达 |

### 2.2 架构 / 双写 / 上帝函数

#### P1-18 `mergeRelationships` + `_pushRelationshipsToGM` 跨 3 文件双写链
- **文件**：[systems.js:865-899](file:///workspace/js/systems.js#L865-L899) + [ai-response-mutator.js:627-632](file:///workspace/js/ai-contract/ai-response-mutator.js#L627-L632) + [phone-ui.js:5066](file:///workspace/js/phone-ui.js#L5066) + [core.js:323](file:///workspace/js/core.js#L323)（_pushRelationshipsToGM 定义）
- **类型**：架构 / 双写
- **描述**：`systems.js` 的 `mergeRelationships` 先调 `RelationshipMutator.mergeRelationships`（写 StateManager.entities.relationships → 镜像到 gameState.relationships），紧接着又调 `_pushRelationshipsToGM()`（把 gameState.relationships 反推到 gm.tables.relationships）。同一双写模式在 `ai-response-mutator.js:627` 和 `phone-ui.js:5066` 重复出现。core.js:46 注释声称 `gm.tables.* 是权威源`，但 RelationshipMutator 写的是 StateManager —— 两个并行存储源，权威性混淆。
- **修复**：明确唯一权威源（建议 StateManager.entities.*），删除 `_pushRelationshipsToGM` 整条 legacy 链路，让 EnhancedMemory 直接从 StateManager 读。
- **证据**：Grep `_pushRelationshipsToGM` 命中 systems.js:898 + ai-response-mutator.js:628 + phone-ui.js:5066 三处调用 + core.js:323 一处定义。

#### P1-19 `sendAIRequest` 1286 行上帝函数
- **文件**：[game.js:920-2206](file:///workspace/js/game.js#L920-L2206)
- **类型**：架构 / 可维护性
- **描述**：单函数承担：撤销快照、世界书扫描、prompt 重建、消息列表构建、深度注入、宏处理、正则应用、上下文裁剪、流式解析、COT 提取、记忆编辑、状态写入（角色/任务/关系/货币/事件/快照）、标题防御、时间防御、选项生成、打字机推送、统计、存档、事件广播。远超 200 行阈值（1286 行）。
- **修复**：拆为 `buildMessages` / `applyAIResponse` / `renderAIResponse` / `postTurnStats` 四阶段。
- **证据**：函数体 920→2206。

#### P1-20 worldSnapshot 写入用 REPLACE 语义，擦除 `_parseStructuredSummary` 写入的 summary/lastUpdate
- **文件**：[game.js:1955-1982](file:///workspace/js/game.js#L1955-L1982) vs [game.js:2465-2472](file:///workspace/js/game.js#L2465-L2472)
- **类型**：正确性 / 数据不一致
- **描述**：`_parseStructuredSummary`（2465）用 MERGE 语义向 worldSnapshot 写入 `{lastUpdate, summary}`。但 `sendAIRequest` 的 1955-1982 用 `var snapshot = {}; ... StateManager.set('ui.worldSnapshot', snapshot)` 整体替换，下一次 AI 回合会把 summary/lastUpdate 字段抹掉。
- **修复**：1955 处改为 `var snapshot = StateManager.get('ui.worldSnapshot') || {};` 再局部更新，保持 MERGE 语义。
- **证据**：game.js:1955 `var snapshot = {}`；game.js:2468 `var _ws = StateManager.get('ui.worldSnapshot') || {}`。

#### P1-21 worldSnapshot 与 StateManager.entities 双数据源
- **文件**：[game.js:1955-1982](file:///workspace/js/game.js#L1955-L1982)、[game.js:1192-1209](file:///workspace/js/game.js#L1192-L1209)
- **类型**：架构 / 双写
- **描述**：worldSnapshot.player 来自 `data.player`（AI 原始返回），worldSnapshot.characters 来自 `gameState.allCharacters` 映射。但权威源是 `StateManager.entities.player` / `entities.characters`。两套数据并行存在，AI 返回的 player 与 entities.player 可能不一致（mutator 可能 normalize）。phone-ui.js:360/449/4690、tavern-compat.js:27/51/377、macro-engine.js:662/670/678 全部读 worldSnapshot，而 game.js 其他位置读 entities —— 读路径分裂。
- **修复**：worldSnapshot 退化为运行时缓存，统一从 StateManager.entities 派生。
- **证据**：30+ 处读 `worldSnapshot.characters/player`，与 entities.* 并存。

#### P1-22 `phone-ui.js` 17 处 `cloneNode(true) + replaceChild + addEventListener` 三步反模式
- **文件**：[phone-ui.js:3616-3618](file:///workspace/js/phone-ui.js#L3616-L3618)、3646、3661、3709、5464、5486、5504、5523、5581、5606、5615、5650、5675、5702、5711
- **类型**：重复 / 性能 / 可维护性
- **描述**：`showApiDetail` 和 `showPresetAction` 大量使用 `el.cloneNode(true) + parentNode.replaceChild + addEventListener` 三步模式来"清除旧监听器"。问题：(1) 每次 `cloneNode(true)` 复制整个子树，`showApiDetail` 一次调用 8 处 cloneNode；(2) 旧节点脱离 DOM 但闭包（如 `_testAbortCtrl`）可能仍持有引用，存在内存泄漏；(3) **项目已提供 `bindFresh(id, event, handler)` 工具函数**，`showApiDetail` 自身就混用两套——部分按钮用 `bindFresh`，部分用 `cloneNode`，风格严重不一致；(4) 行 5452-5460 的取消测试按钮甚至没走 cloneNode 直接 addEventListener，多次打开会累积监听器。
- **修复**：全部迁移到 `bindFresh`；删除所有 cloneNode 三步模式。
- **证据**：Grep `cloneNode\(true\)` 在 phone-ui.js 命中 17 处；`bindFresh` 已存在并被部分使用。

#### P1-23 `phone-ui.js` 12+ 处空状态 SVG 内联重复 ✓ 已修复
- **文件**：[phone-ui.js](file:///workspace/js/phone-ui.js) 多个 `render*Page` 函数 + [systems.js:506](file:///workspace/js/systems.js#L506)
- **类型**：重复 / 可维护性 / a11y
- **描述**：Grep `empty-state-icon` 命中 12+ 处，每处都是 `<div class="empty-state"><div class="empty-state-icon"><svg width="48" height="48" viewBox="0 0 24 24" ...>...</svg></div><p>暂无X</p>...</div>` 的近似重复。修改 SVG 图标或样式需要改 12+ 处，且 SVG **均无 `aria-label`**，屏幕阅读器读不出含义。
- **修复**：✓ 已抽取 `renderEmptyState(iconType, title, hint, options)` 工具函数到 [utils.js:613](file:///workspace/js/utils.js#L613)，9 种预定义 SVG（globe/mail/cart/backpack/book/users/trophy/clock/search）+ 文本字符图标 + textOnly 模式；SVG 加 `role="img" aria-label="..."`。phone-ui.js 15 处 + systems.js 3 处共 18 处已迁移。3 处特殊模式（自定义 div 结构 / 唯一 `<p>` 语义 / DOM 创建）保留原样。
- **证据**：Grep `empty-state-icon` 命中 12 处。

#### P1-24 `MemoryManagerUI` save* 函数双写 Mutator + gm.tables（事务性回滚不彻底）
- **文件**：[tavern-compat.js:4534-4561](file:///workspace/js/tavern-compat.js#L4534-L4561)（saveItem）、4613-4652、4693-4744、4770-4805
- **类型**：架构 / 正确性
- **描述**：这些函数都先调 Mutator（事务性），成功后再手动改 `gm.tables.*` 补齐运行时字段。问题：(1) Mutator 成功后改 gm.tables 失败时无回滚；(2) 双写职责重叠（Mutator 适配器已 MERGE 实体字段到 gm.tables，saveItem 又手动改一遍）；(3) rename 处理分散在 saveItem/saveLocation 各处，逻辑重复且易漏。
- **修复**：Mutator 适配器统一负责 `gm.tables` 的所有字段写入，MemoryManagerUI 只调 Mutator + 显式 `gm._ltmDirty = true`；删除手动改 gm.tables 的代码。
- **证据**：tavern-compat.js:4534-4561 saveItem 双写；4693-4744 saveLocation 双写；行 3855-3876 注释承认"冗余"。

#### P1-25 `_updateDormantStatus` 短名称边界检查不一致（误匹配子串）
- **文件**：[tavern-compat.js:1366-1375](file:///workspace/js/tavern-compat.js#L1366-L1375)
- **类型**：正确性
- **描述**：`isMentioned` 函数对短名称（≤2 字）用边界检查 `\b`，但对长名称直接 `indexOf`，可能误匹配子串。如 NPC 名"小明"会匹配到"小明月"、"李明"会匹配到"李明星"。会导致休眠追踪系统误判角色被提及，不触发休眠提醒。
- **修复**：统一用边界检查或全文匹配；对中文场景 `\b` 不可靠，应改用 `(?:^|[^\u4e00-\u9fa5])` + name + `(?:[^\u4e00-\u9fa5]|$)`。
- **证据**：tavern-compat.js:1366-1375 已读确认。

#### P1-26 `recordCompression` 用 `gameState._stats.totalTurns` 而非 `self.currentTurn`
- **文件**：[tavern-compat.js:3367](file:///workspace/js/tavern-compat.js#L3367)
- **类型**：正确性
- **描述**：`var currentTurn = (typeof gameState !== 'undefined' && gameState._stats) ? (gameState._stats.totalTurns || 0) : self.currentTurn;` —— GameMemory 内部其他地方都用 `self.currentTurn`，这里却读 `gameState._stats.totalTurns`。两者可能不同步：`gameState._stats` 是镜像字段，`_syncLegacyMirror` 是延后的；且 `gameState._stats.totalTurns` 与 `self.currentTurn` 的更新时机不同。
- **修复**：统一用 `self.currentTurn`。
- **证据**：Grep `self.currentTurn` 在 tavern-compat.js 命中 30+ 处，唯独 `recordCompression` 用 `gameState._stats.totalTurns`。

#### P1-27 `core.js` 顶层全局变量跨文件共享
- **文件**：[core.js:2026-2330](file:///workspace/js/core.js#L2026-L2330) + game.js:2757-2758
- **类型**：架构
- **描述**：`_streamModeLocked` / `_streamMode` / `_streamFullText` 以 `var` 全局形式跨 3 个文件共享，无命名空间、无模块封装、无统一守卫。这是典型的"全局变量耦合"反模式。
- **修复**：收拢到 `RuntimeState`（已有该机制）或新建 `StreamState` 单例，提供 `getMode()/setMode()/reset()` 方法。
- **证据**：见 P0-3。

#### P1-28 `UI` 上帝对象（~490 行，承担 6+ 职责）
- **文件**：[core.js:380-870](file:///workspace/js/core.js#L380-L870)
- **类型**：架构 / 可维护性
- **描述**：`UI` 对象混合了 `TOAST_DURATION` 常量、`_navStack` 导航栈、`_modalStack` 模态栈、焦点陷阱、`createModal/hideModal`、`confirm/prompt`、`showGenerating/hideGenerating` 加载态等至少 6 类职责，单对象跨度约 490 行。
- **修复**：拆分为 `ModalManager` / `ToastManager` / `NavigationManager` / `LoadingIndicator` 等独立小对象，`UI` 作为外观转发。

#### P1-29 `formatStory` 247 行 + `requestNpcReply` 226 行 + `ensureLogFallbacks` 268 行 + `injectPresetGlobalVars` 225 行
- **文件**：[game.js:2933-3180](file:///workspace/js/game.js#L2933-L3180)、[game.js:3821-4047](file:///workspace/js/game.js#L3821-L4047)、[game.js:4054-4322](file:///workspace/js/game.js#L4054-L4322)、[game.js:644-869](file:///workspace/js/game.js#L644-L869)
- **类型**：可维护性
- **描述**：四个超长函数，均 >200 行。`formatStory` 内嵌心声提取、段落分割、对话占位、加粗斜体、章节结尾、HTML 净化等 6 个职责。
- **修复**：按职责抽取 `_extractThoughts` / `_renderParagraphs` / `_stripDecorTags` 等子函数。

#### P1-30 `AchievementSystem.calculateStats` 全部直读 gameState，绕过 StateManager
- **文件**：[systems.js:365-398](file:///workspace/js/systems.js#L365-L398)
- **类型**：架构
- **描述**：读 `gameState.conversationHistory`、`gameState.allCharacters`、`gameState.currentBag`、`gameState.relationships` —— 全部直读 legacy 字段，未走 StateManager.get。与 systems.js:16（QuestSystem 走 StateManager）风格不一致。在 StateManager 已是权威源后，直读 gameState 可能拿到未同步的旧值。
- **修复**：统一改 `StateManager.get('progress.conversationHistory' / 'entities.characters' / ...)`。

#### P1-31 base.css 与 systems.css 整段重复定义世界书增强样式
- **文件**：[base.css:1758-1768](file:///workspace/css/base.css#L1758-L1768) ↔ [systems.css:704-709](file:///workspace/css/systems.css#L704-L709)
- **类型**：重复
- **描述**：6 个选择器声明**逐字符完全相同**：`.modal-enhanced .modal-header h3`、`.wi-badge`、`.wi-toolbar`、`.wi-toolbar-btn`、`.wi-toolbar-btn:active`、`.wi-toolbar-btn svg`。systems.css:703 注释自承"已在 base.css 中定义，此处仅保留补充样式"，但实际并无可补充差异，纯属冗余拷贝。
- **修复**：删除 systems.css:704-709 这 6 行。
- **证据**：base.css:1758 与 systems.css:704 完全一致。

#### P1-32 `.chat-item` / `.chat-avatar` 跨文件分裂式定义
- **文件**：[base.css:1574-1598](file:///workspace/css/base.css#L1574-L1598)、[base.css:1637](file:///workspace/css/base.css#L1637) ↔ [phone-ui.css:200-232](file:///workspace/css/phone-ui.css#L200-L232)
- **类型**：重复 / 可维护性
- **描述**：base.css 将 `.chat-item` 与 `.forum-hot-item`/`.mail-list-item` 等合并为共享选择器（`padding:12px 15px`）；phone-ui.css:200-202 又单独覆盖 `.chat-item { padding: 14px 16px }`，破坏了 base.css 的统一间距约定。`.chat-avatar` 同样分裂。
- **修复**：将 `.chat-avatar` 的几何/颜色属性合并进 base.css 的统一区块；删除 phone-ui.css:200-202 的 `.chat-item { padding }` 覆盖。

#### P1-33 4 套响应式断点在 4 个 CSS 文件中各自重复定义
- **文件**：[base.css:1771/1787/1805/1928/2057/2262](file:///workspace/css/base.css#L1771)、[menu.css:570/763/959/1157](file:///workspace/css/menu.css#L570)、pages.css、[phone-ui.css:2712](file:///workspace/css/phone-ui.css#L2712)
- **类型**：重复 / 架构
- **描述**：项目使用 4 个断点（max-width:374、min-width:414、min-width:480、min-width:768），但每个 CSS 文件都独立写一遍 `@media` 块。base.css 单文件就有 6 个 `@media` 块（含 max-width:480/360/374 三种 max-width，语义混乱）。同时存在 `max-width:480` 与 `min-width:480` 边界语义冲突。一旦断点策略变更需改 4 个文件。
- **修复**：抽离 `responsive.css` 统一管理所有 `@media` 覆盖；或采用 mobile-first 单向 `min-width` 链。

#### P1-34 phone-ui.css 119 处硬编码 `border-radius` 数值
- **文件**：[phone-ui.css](file:///workspace/css/phone-ui.css) 全文
- **类型**：硬编码 / 可维护性
- **描述**：Grep 统计 `border-radius:\s*\d` 命中 119 次，全部使用裸数值（4/6/8/9/12/14/16/20px 等），未引用 `:root` 已定义的 `--radius-sm:12px` / `--radius-md:16px` / `--radius-lg:20px` / `--radius-icon:18px`。圆角设计语言无法通过修改变量统一调整，暗色模式或主题切换时也无法联动。
- **修复**：按数值归类映射到变量：4/6px→新增 `--radius-xs`；8px→已有 `--radius-btn`；12/14/16/20px→对应 `--radius-sm/md/lg`。

#### P1-35 `_useSysprompt` 处理逻辑三处重复
- **文件**：[game.js:1009-1013](file:///workspace/js/game.js#L1009-L1013)、[game.js:1119-1124](file:///workspace/js/game.js#L1119-L1124)、[game.js:152-167](file:///workspace/js/game.js#L152-L167)
- **类型**：重复
- **描述**：isInit 分支（1009）、主分支（1119）各内联一遍 `if (_useSysprompt !== false) push system else push user`，而 `_applyUseSysprompt`（152）已是封装好的同类逻辑（requestNpcReply:3938 在用）。三份实现并存。
- **修复**：isInit/主分支都改调 `_applyUseSysprompt`。

---

## 第 3 章：P2 中优先级（48 项）—— 重复 / 性能 / 硬编码 / a11y

### 3.1 重复代码与多种实现

#### P2-1 `contextSize` 存在 6 种读取路径与两个真相源
- **文件**：[worldinfo.js:1597](file:///workspace/js/worldinfo.js#L1597)、[phone-ui.js:4684](file:///workspace/js/phone-ui.js#L4684)、[tavern-compat.js:124/1887/3306](file:///workspace/js/tavern-compat.js#L124)、[core.js:4731/5123/5161/5245/5435](file:///workspace/js/core.js#L4731)、[game.js:1479/1581/1583/2243](file:///workspace/js/game.js#L1479) + [utils.js:118](file:///workspace/js/utils.js#L118) + [state/schema.js:27](file:///workspace/js/state/schema.js#L27)
- **类型**：一致性 / 同一功能多种实现
- **描述**：同一语义（模型上下文窗口大小）被两个真相源承载——遗留全局 `gameState.contextSize` 与新状态层 `StateManager.get('world.contextSize')`。不同读取点 fallback 值不一致（8000 vs 8192 vs 动态探测）。
- **修复**：所有读取统一走 `StateManager.get('world.contextSize')`，单一 fallback 8000。

#### P2-2 思维链剥离双实现，标签集合不一致
- **文件**：[response-parser.js:122](file:///workspace/js/ai-contract/response-parser.js#L122) vs [output-sanitizer.js:28](file:///workspace/js/ai-contract/output-sanitizer.js#L28)
- **类型**：重复实现 / 正确性隐患
- **描述**：`response-parser._stripThinkingTokens` 处理 `think/thinking/reasoning/thought/analysis` 标签；`output-sanitizer.stripThinking` 处理 `<thinking>/<ECoT>/<analysis>/💭`。标签集合**不同**，同一段文本经过两个函数结果可能不同。
- **修复**：统一标签集合到 `OutputSanitizer.stripThinking`，response-parser 内部调用它。

#### P2-3 角色属性归一化双实现
- **文件**：[character-mutator.js:204](file:///workspace/js/state/mutators/character-mutator.js#L204) vs [ai-output-schema.js](file:///workspace/js/ai-contract/schemas/ai-output-schema.js)
- **类型**：重复实现
- **描述**：`CharacterMutator.normalizeStats` 将 stats 归一化为 `{label, value}`，注释明确写"与 ai-output-schema.js 的 player.stats 归一化保持一致"。两份代码各自维护。
- **修复**：抽取为共享工具函数（如 `StateSchema.normalizeStats`）。

#### P2-4 关系合并逻辑双实现
- **文件**：[ai-response-mutator.js:533-622](file:///workspace/js/ai-contract/ai-response-mutator.js#L533-L622) vs [relationship-mutator.js:14-38](file:///workspace/js/state/mutators/relationship-mutator.js#L14-L38)
- **类型**：重复实现
- **描述**：`AIResponseMutator._applyRelationships` 内部 line 601-622 手写"双向匹配 + 上限 10 条"合并逻辑，与 `RelationshipMutator.mergeRelationships` 的核心逻辑（line 18-37）完全重复。注释虽声称"统一收敛"，但实际未调用 RelationshipMutator。
- **修复**：`_applyRelationships` 改为调用 `RelationshipMutator.mergeRelationships(graphEntries, { silent: true })`。

#### P2-5 `mergeCharacters` 主角过滤逻辑与 AIResponseMutator._applyCharacters 重复
- **文件**：[game.js:3361-3377](file:///workspace/js/game.js#L3361-L3377) vs [ai-response-mutator.js:238-244](file:///workspace/js/ai-contract/ai-response-mutator.js#L238-L244)
- **类型**：重复
- **描述**：两处都做 `chars.filter → name 非空 → 排除 undefined/null → 排除主角`。过滤策略完全一致。
- **修复**：抽取 `CharacterMutator._filterOutPlayer(chars)` 公共方法。

#### P2-6 `extractStr/extractArr/extractObjArr` 每次调用都 `new RegExp`，已有缓存却未用
- **文件**：[core.js:2595-2713](file:///workspace/js/core.js#L2595-L2713)
- **类型**：性能 / 冗余
- **描述**：四个 extract* 函数每次都执行 `text.match(new RegExp(...))`，在流式解析热路径上反复重编译。而 `_getCachedRegExp`（2586）本就是为此而写的缓存，却无人调用（见 P1-2）。
- **修复**：将 extract* 内的 `new RegExp(...)` 替换为 `_getCachedRegExp(...)`，一举激活缓存并消除 P1-2 的死代码。

#### P2-7 8 个 `parse*Content` 函数结构高度雷同
- **文件**：[core.js:3604-3769](file:///workspace/js/core.js#L3604-L3769)
- **类型**：重复代码
- **描述**：`parseForumContent` / `parseChatContent` / `parseCalendarContent` / `parseMailContent` / `parseShopContent` / `parseMomentsContent` / `parseItemsContent` / `parseDiaryContent` 共 8 个函数，每个都遵循"try → parseJSONHelper → extract 字段 → 构造返回对象 → catch 返回 null"的相同骨架。
- **修复**：抽取 `parseStructuredContent(schema)` 通用函数，用声明式 schema 描述差异。

#### P2-8 `phone-ui.js` 4 处 `firstElementChild; if (child) { flex/minHeight }` 样板重复
- **文件**：[phone-ui.js:1971](file:///workspace/js/phone-ui.js#L1971)、[phone-ui.js:1991](file:///workspace/js/phone-ui.js#L1991)、[phone-ui.js:2719](file:///workspace/js/phone-ui.js#L2719)
- **类型**：重复
- **修复**：抽取 `_stretchFirstChild(container)` 工具函数。

#### P2-9 `Math.max(0, Math.min(100, 50 + fav / 2))` 重复 2 处
- **文件**：[phone-ui.js:7229](file:///workspace/js/phone-ui.js#L7229)、[phone-ui.js:7335](file:///workspace/js/phone-ui.js#L7335)
- **类型**：重复
- **修复**：抽 `favToWidth(fav)` 工具函数。

#### P2-10 `@keyframes spin` 与 `@keyframes app-loading-spin` 重复定义旋转动画
- **文件**：[pages.css:354-358](file:///workspace/css/pages.css#L354-L358) ↔ base.css:2600 区域
- **类型**：重复
- **修复**：统一为 `@keyframes spin`，删除 `app-loading-spin` 别名。

### 3.2 性能问题

#### P2-11 `macro-engine timestamp` 循环内 new RegExp
- **文件**：[macro-engine.js:590](file:///workspace/js/modules/macro-engine.js#L590)
- **类型**：性能
- **描述**：`timestamp` 函数每次调用都在 `keys.forEach` 内执行 `new RegExp(k, 'g')`，对 12 个格式占位符各编译一次正则。该函数被 `{{timestamp:FORMAT}}` 宏调用，每条消息渲染都可能触发。
- **修复**：将 `map` 的键预编译为正则并缓存到模块级常量。

#### P2-12 `worldinfo getAllEnabledEntries` 每次 scan 全量构建
- **文件**：[worldinfo.js:49-60](file:///workspace/js/worldinfo.js#L49-L60)
- **类型**：性能
- **描述**：`getAllEnabledEntries` 遍历所有 books 的 entries 构建新对象。在 `scan`（line 1430）和 `recursiveScan` 的每一步（line 1719）都调用，递归扫描 3 步意味着 4 次全量构建。条目数量大时（如 500+）会有明显开销。
- **修复**：scan 入口处构建一次 `allEntries`，作为参数传递给 recursiveScan。

#### P2-13 `worldinfo matchKeys` 全词匹配路径无缓存（matchKeysAll 有缓存）
- **文件**：[worldinfo.js:1604-1625](file:///workspace/js/worldinfo.js#L1604-L1625) vs [worldinfo.js:1673-1697](file:///workspace/js/worldinfo.js#L1673-L1697)
- **类型**：性能 / 重复
- **描述**：`matchKeysAll`（1685-1691）使用 `self._regexCache` 缓存全词匹配正则，但 `matchKeys`（1618-1624）的全词匹配路径每次都 `new RegExp`。两个函数处理同一种匹配场景，缓存策略不一致。
- **修复**：matchKeys 全词匹配路径也使用 `_regexCache`。

#### P2-14 `preset-manager parsePreset prompt_order` 重复 find
- **文件**：[preset-manager.js:558-559](file:///workspace/js/modules/preset-manager.js#L558-L559)、[preset-manager.js:665-666](file:///workspace/js/modules/preset-manager.js#L665-L666)
- **类型**：性能 / 冗余
- **描述**：同一个 `data.prompt_order.find(g => g.character_id === 100000) || data.prompt_order.find(g => g != null)` 查找逻辑在 parsePreset 中出现两次，相隔 100 行但逻辑完全相同。
- **修复**：提取为局部变量 `var orderGroup = ...`，在 line 558 处计算后复用。

#### P2-15 `cotRegex` / `decorTags` / `_looksLikeInitialScene` 内联正则每次请求重编译
- **文件**：[game.js:1674](file:///workspace/js/game.js#L1674)、[game.js:1376](file:///workspace/js/game.js#L1376)、[game.js:2888-2905](file:///workspace/js/game.js#L2888-L2905)
- **类型**：性能
- **描述**：作为正则字面量写在 sendAIRequest 函数体内。每次函数调用仍需重新创建 RegExp 对象。sendAIRequest 每回合调用，长会话累积。
- **修复**：提升为模块级常量。

#### P2-16 `phone-ui.js` `Object.values(gameState.allCharacters)` 多次
- **文件**：[phone-ui.js:7166](file:///workspace/js/phone-ui.js#L7166)、[phone-ui.js:2919](file:///workspace/js/phone-ui.js#L2919)、[phone-ui.js:2976](file:///workspace/js/phone-ui.js#L2976)
- **类型**：性能
- **描述**：每帧调用 + 双重遍历。
- **修复**：缓存 `allCharactersArray`，在 StateManager 订阅变更时更新。

#### P2-17 `renderApiSettings` 每次全量重渲染
- **文件**：[phone-ui.js:5068-5160](file:///workspace/js/phone-ui.js#L5068-L5160)
- **类型**：性能
- **描述**：100 个 API 时全量 DOM 重建。
- **修复**：分组前先按 group 索引卡片，增量更新。

#### P2-18 `AchievementSystem.checkAchievements` 每回合对每个成就重新 match 正则
- **文件**：[systems.js:413](file:///workspace/js/systems.js#L413)
- **类型**：性能
- **描述**：`cond.match(/^(\w+)\s*(>=|<=|>|<|==|!=)\s*(\d+)$/)` 在 forEach 内调用。每回合 × 每成就仍重复 match。
- **修复**：启动时预解析 condition → {field, op, val} 缓存到 ach._parsedCond。

#### P2-19 `changeNpcAvatar` 头像压缩未清理旧 dataURL（内存泄漏）
- **文件**：[phone-ui.js:6716-6773](file:///workspace/js/phone-ui.js#L6716-L6773)
- **类型**：性能 / 内存
- **描述**：直接覆盖旧 dataURL。旧 dataURL（可能数百 KB）若被其他 DOM 元素引用，不会立即 GC；且 `gameState._npcAvatars` 无大小上限，频繁换头像会累积。
- **修复**：覆盖前显式置旧值为 null；限制 `_npcAvatars` 总大小；考虑用 IndexedDB 存图片。

### 3.3 硬编码 / 可维护性

#### P2-20 `permanentFacts` 6 个 list 字段无数量上限 / token 上限
- **文件**：[tavern-compat.js:1748-1832](file:///workspace/js/tavern-compat.js#L1748-L1832)（push 点）、[tavern-compat.js:2150-2199](file:///workspace/js/tavern-compat.js#L2150-L2199)（_buildPermanentFactsSection）
- **类型**：性能 / 可维护性
- **描述**：`permanentFacts.pcIdentity / worldRules / settings / npcProfiles / promises / worldPlaces` 都是数组，但**没有任何数量上限**。`_buildPermanentFactsSection` 直接 `list.forEach(a => lines.push(a.content))` 全量输出。如果 AI 持续 push，list 无限增长，最终 token 预算被永久事实区独占。对比：`worldAnchors` 有 30 条上限 + 权重淘汰；`importantEvents` 有 60/40 预算 + decay 修剪；`timeline` 有 50 条上限；唯独 `permanentFacts` 6 个 list 无上限。
- **修复**：为每个 `permanentFacts` list 加数量上限（如 `npcProfiles` 30 条，其他 20 条），或按 token 预算 slice。

#### P2-21 `THEME_LIBRARY` 硬编码主题数据约 180 行
- **文件**：[core.js:1622-1801](file:///workspace/js/core.js#L1622-L1801)
- **类型**：可维护性 / 架构
- **描述**：主题库以数组字面量直接嵌入 core.js，新增主题需改动核心文件。
- **修复**：抽取到 `data/themes.js` 或 `themes.json`。

#### P2-22 `_mapTheaterByKey` / `_createModuleFromType` 超长 switch（合计 330 行）
- **文件**：[core.js:3001-3330](file:///workspace/js/core.js#L3001-L3330)
- **类型**：可维护性
- **描述**：`_mapTheaterByKey` 含 50+ `case` 标签，`_createModuleFromType` 跨度约 330 行，是典型的 switch dispatch 反模式。
- **修复**：改为 `Map<string, mapper>` 查表 + 默认 fallback。

#### P2-23 `translateError` 错误映射表 100+ 条目内嵌
- **文件**：[core.js:3778-3985](file:///workspace/js/core.js#L3778-L3985)
- **类型**：可维护性
- **描述**：错误映射表与翻译函数共占约 200 行，混杂数据与逻辑。且 `httpMatch` 路径先于 `substring` 路径执行，维护者新增条目时易忽略优先级。
- **修复**：映射表抽到 `data/error-map.js`。

#### P2-24 `phone-ui.js` `showApiDetail` 函数过长（427 行）
- **文件**：[phone-ui.js:5230-5656](file:///workspace/js/phone-ui.js#L5230-L5656)
- **类型**：可维护性
- **修复**：拆分为 `_bindApiDetailButtons(slot)` + `_renderApiDetailStats(slot)` + `_renderApiDetailErrors(slot)` 等子函数。

#### P2-25 `phone-ui.js` `_restoreGameRender` 函数过长（170 行）+ 三处默认 choices 重复
- **文件**：[phone-ui.js:4774-4943](file:///workspace/js/phone-ui.js#L4774-L4943)
- **类型**：可维护性
- **修复**：抽取 `_renderDefaultChoices(variant)`；函数拆分为 `_restoreLastAIReply` + `_restoreFallbackUI`。

#### P2-26 `phone-ui.js` 大量魔法数字
- **文件**：[phone-ui.js:6737](file:///workspace/js/phone-ui.js#L6737)（512 头像尺寸）、[phone-ui.js:6725](file:///workspace/js/phone-ui.js#L6725)（2MB 头像限制）、[phone-ui.js:7055](file:///workspace/js/phone-ui.js#L7055)（100/50 阈值）、[phone-ui.js:5088](file:///workspace/js/phone-ui.js#L5088)（UndoMutator 50 上限）、[phone-ui.js:7229](file:///workspace/js/phone-ui.js#L7229)（50+fav/2）
- **类型**：可维护性
- **修复**：抽常量：`AVATAR_MAX_DIM = 512`、`AVATAR_MAX_SIZE = 2*1024*1024`、`NPC_CHAT_HISTORY_MAX = 100`、`UNDO_HISTORY_LIMIT = 50`。

#### P2-27 `phone-ui.js` 大量内联 onclick（a11y + CSP + XSS 三重风险）
- **文件**：[phone-ui.js:321](file:///workspace/js/phone-ui.js#L321)、5171、6423、6436、6444、6475、6476、6479、6509 等
- **类型**：a11y / 性能 / 安全
- **描述**：项目已迁移部分到 `data-action` + 事件委托，但仍有大量内联 onclick。问题：(1) 无 `role`/`tabindex`，键盘用户无法访问；(2) CSP 严格模式下需要 `'unsafe-inline'`；(3) 字符串拼接易引入 XSS。
- **修复**：全面迁移到事件委托（`data-action` + 统一处理器）。

#### P2-28 `phone-ui.js` 内联 SVG 无 `aria-label`（12+ 处）
- **文件**：[phone-ui.js](file:///workspace/js/phone-ui.js) 12+ 处 `empty-state-icon`
- **类型**：a11y
- **修复**：SVG 加 `role="img" aria-label="暂无X"`。

#### P2-29 `tavern-compat.js` `_ltmDirty` 设置点散落 18 处，无统一入口
- **文件**：[tavern-compat.js:1080](file:///workspace/js/tavern-compat.js#L1080)、1845、2833、2845、2859、2864、2867、3065、3074、3478、3590、3606、3645、3672、3694、3818、4268、4290
- **类型**：可维护性
- **描述**：`_ltmDirty = true` 散落 18 处，新增写入点极易遗漏（如 P0-7 的 MemoryManagerUI 各 save* 函数就漏了）。
- **修复**：提供 `gm._markLtmDirty()` 方法，所有写入点统一调用。

#### P2-30 `phone-ui.js` `saveNpcEdit` 直写 `gameState.conversationHistory.push`
- **文件**：[phone-ui.js:7160-7166](file:///workspace/js/phone-ui.js#L7160-L7166)
- **类型**：架构
- **描述**：直接 `gameState.conversationHistory.push({...})` 注入"已更新角色设定"消息。绕过 `StateManager.set`，导致：(1) `_syncLegacyMirror` 不触发，StateManager 内部值陈旧；(2) 注入的消息不进入撤销历史。
- **修复**：改走 `StateManager.set('progress.conversationHistory', [...])` 或提供 `ConversationMutator.append(msgs)` API。

#### P2-31 `mergeQuests` legacy 分支直接操纵 gameState.currentQuests，绕过 StateManager
- **文件**：[systems.js:704-770](file:///workspace/js/systems.js#L704-L770)
- **类型**：架构 / 双写
- **描述**：当 `QuestMutator` 不可用时，legacy 分支直接 `gameState.currentQuests.push/splice`，再调 `_pushCurrentQuestsToGM`。且 statusMap/typeMap 标准化逻辑与 QuestMutator 内部 normalize 重复实现。
- **修复**：删除 legacy 分支，QuestMutator 不可用时直接抛错。

#### P2-32 `buildSystemPrompt` 仍有双拼装路径
- **文件**：[game.js:483-527](file:///workspace/js/game.js#L483-L527)
- **类型**：重复 / 架构
- **描述**：PromptBuilder 可用时走 `PromptBuilder.buildSystemPrompt(ctx)`；但 `includeFormatRules=false` 时又手动 `[_setupText, _narrativeEnhancement, _memoryText..., _chatContextText, _formatAnchor].filter(Boolean).join('\n\n')` 拼装，与 PromptBuilder section 注册机制产出不同。preset-manager.js:1374 调 `buildSystemPrompt(false)` 会走这条手动路径。
- **修复**：`includeFormatRules=false` 也走 PromptBuilder，通过 mode/flag 控制是否注入 formatRules section。

#### P2-33 `QuestSystem.getAllQuests` 在 StateManager.get 返回 undefined 时崩溃
- **文件**：[systems.js:15-26](file:///workspace/js/systems.js#L15-L26)
- **类型**：正确性
- **描述**：`var quests = (StateManager ? StateManager.get('entities.quests') : (gameState.currentQuests || []));` —— StateManager 存在但 `entities.quests` 未初始化时，`StateManager.get` 返回 `deepClone(undefined)` = undefined。随后 `quests.filter(...)` 抛 TypeError。fallback `|| []` 只在 StateManager 为 falsy 时生效，条件写反。
- **修复**：`StateManager.get('entities.quests') || (gameState.currentQuests || [])`。

#### P2-34 已完成的引导任务被永久推入列表
- **文件**：[systems.js:15-26](file:///workspace/js/systems.js#L15-L26)
- **类型**：正确性
- **描述**：`getAllQuests` 检测"无 ACTIVE 任务"时 push `_cachedGuidanceQuest`。但缓存对象在 `advanceGuidanceQuest` 完成后 status 变为 COMPLETED，下次仍判定"无 ACTIVE"→ 再次 push 同一个已完成对象。玩家会一直看到"继续探索 - 已完成"。
- **修复**：完成后 `_cachedGuidanceQuest = null` 让下轮重新创建；或 push 前检查 `q.status === ACTIVE`。

#### P2-35 `onStreamChunk` 中 `var story` 重复声明 + 逻辑分支冗余
- **文件**：[game.js:2781](file:///workspace/js/game.js#L2781)、[game.js:2792](file:///workspace/js/game.js#L2792)
- **类型**：冗余
- **修复**：2781 改用不同变量名或直接复用。

#### P2-36 `currentTokens` 变量在 sendAIRequest 中语义复用
- **文件**：[game.js:1487](file:///workspace/js/game.js#L1487)（输入 token）、[game.js:2082](file:///workspace/js/game.js#L2082)（输出 token）
- **类型**：可维护性
- **修复**：2082 改名 `outputTokens`。

### 3.4 CSS 硬编码与 a11y

#### P2-37 phone-ui.css 21 处硬编码 `#fff` / `#000` 颜色
- **文件**：[phone-ui.css:132/140/211/222/230/715/843/845/855/900/925/1044/1253/1269/1332/1759/1760/1789/1790/1950/1975/2014/2354/2510/2525](file:///workspace/css/phone-ui.css#L132)
- **类型**：硬编码
- **描述**：21 处直接写死 `#fff`/`#000`，未走 `var(--card)`/`var(--text)`/`var(--bg)`。在暗色模式下这些白底文字/黑底容器不会反转，导致 phone-ui 子页面在暗色模式下出现"白底黑字"的违和块。典型点：1759 `.diary-card-lock { background:#000 }`、1789 `.diary-memo-tag { background:#000 }`、140/230 `border: 2px solid #fff`（徽章描边在暗色下看不见）。
- **修复**：全部替换为变量：白底→`var(--card)`；纯黑底→`var(--text)` 或新增 `--ink`；徽章描边→`var(--bg)`。

#### P2-38 index.html 内联 `<style>` 块未外置
- **文件**：[index.html:18-30](file:///workspace/index.html#L18-L30)
- **类型**：HTML / 性能
- **描述**：`<head>` 内嵌 12 行 `<style>`，定义 `.eye-item` / `.archetype-card` 等规则，阻塞首屏解析，无法被浏览器缓存。
- **修复**：整段迁移到 base.css 或 systems.css。

#### P2-39 index.html 存在 247 处内联 `style="..."`
- **文件**：[index.html](file:///workspace/index.html) 全文（247 处）
- **类型**：HTML / 可维护性
- **描述**：典型滥用：490 `#autoSaveDot` 内联硬编码颜色、598 头像渐变、617 `#logSubContainer` 内联动画 + 4 个负向偏移魔法数字、840/1740/2120 等 `circle-btn` 反复内联几何属性、2128/2130/2132 重复内联 `background:#EDEDED`。
- **修复**：抽取高频模式为工具类，目标将内联 style 降至 < 30 处。

#### P2-40 `#autoSaveDot` 内联硬编码 `#4caf50`，与 `--success` 变量重复
- **文件**：[index.html:490](file:///workspace/index.html#L490)
- **类型**：硬编码
- **描述**：`background:#4caf50` 与 base.css:25 `--success: #34c759` 语义重复但色值不同。两套"成功色"并存，且暗色模式下 `--success` 会切换为 `#32d74b` 而此点不会。
- **修复**：改为 `background: var(--success)`。

#### P2-41 phone-ui.css 硬编码 `rgba(0,122,255,...)` 蓝色系
- **文件**：[phone-ui.css:35/46](file:///workspace/css/phone-ui.css#L35)
- **类型**：硬编码
- **描述**：`.api-fetch-btn` 边框 `rgba(0, 122, 255, .3)`、hover `rgba(0, 122, 255, .2)`，与 base.css `--rarity-uncommon: #1890ff`（24,144,255）色值不同，属第二套"链接蓝"。
- **修复**：新增 `--accent-blue`/`--accent-blue-soft` 变量统一引用。

#### P2-42 base.css 空 `#storyText {}` 规则块
- **文件**：[base.css:147-148](file:///workspace/css/base.css#L147-L148)
- **类型**：死代码
- **修复**：删除 147-148 两行。

#### P2-43 systems.css 顶部注释残留"已删除 1766 行死代码"说明
- **文件**：[systems.css:1066-1080](file:///workspace/css/systems.css#L1066-L1080)
- **类型**：死代码（注释）
- **修复**：删除该注释段，迁移至 git commit message。

#### P2-44 `meta theme-color` 硬编码亮色，暗色模式不适配
- **文件**：[index.html:6](file:///workspace/index.html#L6)
- **类型**：a11y / HTML
- **修复**：添加 `<meta name="theme-color" content="#1a1a1a" media="(prefers-color-scheme: dark)">`。

#### P2-45 多处 `aria-label="关闭"` 缺少 `type="button"`
- **文件**：[index.html:1187/1730/1812/2162/2198/2211/2527](file:///workspace/index.html#L1187) 等
- **类型**：a11y
- **描述**：大量 `<button class="circle-btn" data-close="...">` 未声明 `type="button"`，在 `<form>` 上下文中默认为 `type="submit"` 可能触发提交。
- **修复**：所有非提交按钮统一添加 `type="button"`。

#### P2-46 `tavern-compat.js` `parseAIEditTags` 缺少 break，依赖 if/else if 链式短路
- **文件**：[tavern-compat.js:1190](file:///workspace/js/tavern-compat.js#L1190)
- **类型**：可维护性
- **修复**：改为 switch + 显式 break/return。

#### P2-47 `phone-ui.js` `renderNpcPage` 渲染缓存 key 漏算 `details` 字段
- **文件**：[phone-ui.js:7188](file:///workspace/js/phone-ui.js#L7188)
- **类型**：正确性 / 性能
- **描述**：`sigParts` 只拼了 `name:favorability:title:relation:desc.length`，没拼 `details`。AI 更新角色 details 后，`RenderCache.same` 仍返回 true，跳过重绘，人际页看不到 details 变化。
- **修复**：`sigParts.push(... + ':' + (_c.details ? _c.details.length : 0))`。

#### P2-48 `phone-ui.js` `editChatRemark` 内联 HTML 转义不完整
- **文件**：[phone-ui.js:6675](file:///workspace/js/phone-ui.js#L6675)
- **类型**：安全
- **描述**：手动转义未处理单引号 `'`，与项目 `escapeAttr` 工具函数重复且不一致。
- **修复**：改用 `escapeAttr(currentRemark)` 统一转义。

---

## 第 4 章：P3 低优先级（32 项）—— 风格 / 命名 / 微优化

### 4.1 风格 / 命名

| 编号 | 文件:行号 | 问题 | 修复 |
|------|----------|------|------|
| P3-1 | [core.js](file:///workspace/js/core.js)（整文件） | 单文件 5564 行 | 按领域拆分为 api.js / stream.js / save.js / ui.js / parse.js / sanitize.js / theater.js / time.js / error-translate.js |
| P3-2 | [core.js:1999-2001](file:///workspace/js/core.js#L1999-L2001)、[phone-ui.js:3891-3893](file:///workspace/js/phone-ui.js#L3891-L3893) | `typeof` 守卫策略不一致 | 统一为有守卫，或统一收拢到命名空间 |
| P3-3 | [core.js:4752-4984](file:///workspace/js/core.js#L4752-L4984) | 三套解析路径（流式/普通/fallback） | 抽取共享的 `parseAIFields(rawText)` 钩子 |
| P3-4 | [core.js:2716](file:///workspace/js/core.js#L2716) | `extractObj` 注释与实际失实 | 删除 `extractObj` 的同时订正该注释 |
| P3-5 | [core.js:2585](file:///workspace/js/core.js#L2585) | `_regexCache` 死代码 | 接入 extract* 激活缓存（见 P2-6） |
| P3-6 | [init.js:7-10](file:///workspace/js/init.js#L7-L10) | 顶部重复注释块 `// 初始化` | 合并为一行注释 |
| P3-7 | [game.js:3267-3270](file:///workspace/js/game.js#L3267-L3270) | `renderHUD` 接收参数但完全忽略 | 删除参数，或改名 `refreshTimeUI` |
| P3-8 | [systems.js:963](file:///workspace/js/systems.js#L963) | `getRelationTagClass` 正则中 `sweetheart` 前多余空格 | 去掉空格或改为 `\s*sweetheart` |
| P3-9 | [systems.js:105-107](file:///workspace/js/systems.js#L105-L107)、[systems.js:143-147](file:///workspace/js/systems.js#L143-L147) | `QuestSystem.filterByStatus` 的 'abandoned' 分支无 UI 入口 | 删除 abandoned 分支或补 UI 按钮 |
| P3-10 | [game.js:2208](file:///workspace/js/game.js#L2208)、[game.js:2232](file:///workspace/js/game.js#L2232)、[game.js:2246](file:///workspace/js/game.js#L2246) | `updateTokenCount` 直读 gameState.conversationHistory | 改读 StateManager |
| P3-11 | [game.js:2250](file:///workspace/js/game.js#L2250) | 缩进错误（多了 4 个前导空格） | 修正缩进 |
| P3-12 | [systems.js:786-859](file:///workspace/js/systems.js#L786-L859) vs [systems.js:159-217](file:///workspace/js/systems.js#L159-L217) | `renderQuests` 与 `QuestSystem.renderQuestList` 渲染逻辑部分重复 | 抽取 `_renderQuestItem(q)` 公共片段 |
| P3-13 | [utils.js](file:///workspace/js/utils.js) | `safeInt("123abc")` 返回 123 | 写文档说明起始数字被吞的行为 |
| P3-14 | [utils.js](file:///workspace/js/utils.js) | `escapeHtml` 没处理反引号 | 加 `` ` `` 转义 |
| P3-15 | [utils.js](file:///workspace/js/utils.js) | `truncateByChars` 中文按字符截断 | 检查 codePoint，避免在代理对中间切 |
| P3-16 | [preset-manager.js:482-488](file:///workspace/js/modules/preset-manager.js#L482-L488) | `safeNum` 函数签名语义混乱（参数个数重载） | 统一为 `safeNum(...candidates, defaultValue)` rest 参数 |
| P3-17 | [character-mutator.js:46-47](file:///workspace/js/state/mutators/character-mutator.js#L46-L47)、97-98、132-133、182-183 | 双写 favorability + favor 字段 | 统一为 `favorability` 单字段，`favor` 由 _syncLegacyMirror 兼容映射 |
| P3-18 | [time-mutator.js:73](file:///workspace/js/state/mutators/time-mutator.js#L73) | `_timeToMinutes` 正则字符类冗余 `::` | 改为 `/(\d{1,2})[:：](\d{2})/` 兼容全角冒号 |
| P3-19 | [stscript-bridge.js:37](file:///workspace/js/ai-contract/stscript-bridge.js#L37)、67-68 | 变量声明顺序混乱 | 声明提升到函数顶部 |
| P3-20 | [worldinfo.js:1936-1953](file:///workspace/js/worldinfo.js#L1936-L1953) | `buildInjectionGrouped` permanentFacts 去重索引每次全量构建 | 在 EnhancedMemory 上维护 `getHarvestedContents()` 方法并内部缓存 |
| P3-21 | [prompt-builder.js:53-57](file:///workspace/js/ai-contract/prompt-builder.js#L53-L57) | `buildUserPrompt` 极简实现，context 参数未使用 | 删除 context 参数简化签名 |
| P3-22 | [state-manager.js:239-253](file:///workspace/js/state/state-manager.js#L239-L253) | `_matchPattern` 对 `**` 中间位置的处理不完整 | 实现严格 glob 风格递归匹配，或注释明确限制 |
| P3-23 | [state-manager.js:11-14](file:///workspace/js/state/state-manager.js#L11-L14) | `_legacyMode` 过时注释残留 | 删除注释，保留 getLegacy/setLegacy 简短说明 |
| P3-24 | [phone-ui.js](file:///workspace/js/phone-ui.js) 全文 | 注释中残留大量历史阶段标记 `【P0-2.6 阶段3-1】` | 保留核心设计注释，删除阶段标记前缀 |
| P3-25 | [phone-ui.js](file:///workspace/js/phone-ui.js) 多处 | 缩进风格混用（4 空格 / 2 空格 / 8 空格） | 统一为 4 空格 |
| P3-26 | [tavern-compat.js:158-417](file:///workspace/js/tavern-compat.js#L158-L417) | 同一函数内 `var`/`let`/`const` 混用 + `var` 重复声明 | 统一用 `let`；`self = this` 提到函数顶部 |
| P3-27 | [tavern-compat.js](file:///workspace/js/tavern-compat.js) 全文 | 注释中英混排 | 风格统一 |
| P3-28 | [base.css:27](file:///workspace/css/base.css#L27) vs [base.css:35](file:///workspace/css/base.css#L35) | `--warning` 变量被重复定义两次 | 删除 27 行的 `--warning: #ffb800` |
| P3-29 | [base.css:322-1326](file:///workspace/css/base.css#L322-L1326) | 工具类与组件类混排，无命名空间隔离 | 工具类统一加 `u-` 前缀 |
| P3-30 | [systems.css:797-798](file:///workspace/css/systems.css#L797-L798) | `tag-output` 用 `--warning` 而 `tag-input` 用 `--success`，语义不一致 | 新增 `--tag-input`/`--tag-output` 语义变量 |
| P3-31 | [phone-ui.css:513-515](file:///workspace/css/phone-ui.css#L513-L515) | `.npc-chat-loading::before { content: '对方正在输入中' }` 硬编码文案 | 改用 `data-loading-text` 属性 |
| P3-32 | [systems.css:707/724/741/743/773](file:///workspace/css/systems.css#L707) | transition 时长硬编码 0.15s | 抽 `--transition-fast: 0.15s` 变量 |

---

## 第 5 章：修复路线建议（按依赖关系排序）

### 阶段 1：立即止血（P0，1-2 天）

| 顺序 | 编号 | 文件 | 修复内容 | 风险 |
|------|------|------|---------|------|
| 1 | P0-1 | utils.js:439-440 | 删除双重错误处理器 | 低 |
| 2 | P0-2 | init.js:344-370 | 删除双重事件委托，合并 toggle-thought | 低 |
| 3 | P0-13 | undo-mutator.js:69 | `transient.conversationHistory` → `progress.conversationHistory` | 低（一行修复） |
| 4 | P0-7 | tavern-compat.js 各 save* | 追加 `gm._ltmDirty = true` | 低 |
| 5 | P0-11 | phone-ui.js:320 | `appendForumReply` 时间显示修复 | 低 |
| 6 | P0-10 | phone-ui.js | moments 朋友圈点赞/评论委托补全 | 低 |
| 7 | P0-6 | game.js:1376/2885 | 统一 danmu/danbu 拼写 | 低 |
| 8 | P0-4 | game.js:1907-1937 | 货币块加 `_doLegacyStateWrites` 守卫 | 中 |
| 9 | P0-5 | game.js:1939-1953 | keyEvents 块加守卫 | 中 |
| 10 | P0-3 | game.js + core.js + phone-ui.js | `_streamMode*` 收拢到 StreamState | 中 |
| 11 | P0-8 | phone-ui.js:903 | `_autoExtractWorldNotes` 改走 API | 中 |
| 12 | P0-14 | stscript-bridge.js:37 | 变量声明提升 | 低 |
| 13 | P0-15 | index.html:9 | 排查 eval 依赖后收紧 CSP | 中 |
| 14 | P0-9 + P0-12 | tavern-compat.js + phone-ui.js | MemoryManagerUI 改走 StateManager | 高（80+ 调用点） |

### 阶段 2：死代码与重复清理（P1-1~P1-17 + P2 重复项，3-5 天）

| 顺序 | 编号 | 文件 | 修复内容 |
|------|------|------|---------|
| 15 | P1-1~P1-17 | 多处 | 删除 17 组死代码（时间函数 5 个、_getCachedRegExp、isSaveLocked、extractObj、Logger.LEVELS、applyParamPreset、deleteCharacter、双重 catch、_customTypes 等） |
| 16 | P2-6 + P1-2 + P3-5 | core.js | extract* 接入 _getCachedRegExp（一举三得） |
| 17 | P1-18 | systems.js + ai-response-mutator.js + phone-ui.js + core.js | 删除 _pushRelationshipsToGM 双写链 |
| 18 | P1-22 | phone-ui.js | cloneNode 三步模式全迁移到 bindFresh |
| 19 | P1-23 + P2-28 | phone-ui.js + systems.js | 抽取 `renderEmptyState(iconType, title, hint)` |
| 20 | P1-31 + P1-32 + P1-33 | CSS | 删除重复样式 + chat-item/avatar 合并 + 响应式抽离 |
| 21 | P1-34 + P2-37 + P2-41 | phone-ui.css | 硬编码 border-radius / #fff / #000 / 蓝色 → CSS 变量 |
| 22 | P2-38 + P2-39 + P2-40 + P2-44 + P2-45 | index.html | 内联 style 外置 + type="button" + theme-color 暗色 |

### 阶段 3：架构收敛（P1-19~P1-30 + P2 重复项，1-2 周）

| 顺序 | 编号 | 文件 | 修复内容 | 风险 |
|------|------|------|---------|------|
| 23 | P1-19 | game.js | `sendAIRequest` 拆为 4 阶段 | 高 |
| 24 | P1-29 | game.js | formatStory / requestNpcReply / ensureLogFallbacks 拆分 | 高 |
| 25 | P1-24 | tavern-compat.js | MemoryManagerUI 只调 Mutator + API | 中 |
| 26 | P1-21 | game.js | worldSnapshot 退化为派生缓存 | 中 |
| 27 | P1-20 | game.js | worldSnapshot 写入改 MERGE 语义 | 低 |
| 28 | P1-30 | systems.js | AchievementSystem 改走 StateManager | 低 |
| 29 | P2-31 | systems.js | 删除 mergeQuests legacy 分支 | 中 |
| 30 | P2-1 | 多处 | contextSize 真相源统一 | 中 |
| 31 | P2-2 + P2-3 + P2-4 + P2-5 | ai-contract + state | 思维链/stats/关系/角色过滤统一 | 中 |
| 32 | P2-20 | tavern-compat.js | permanentFacts 加数量上限 | 低 |
| 33 | P2-29 | tavern-compat.js | `_ltmDirty` 统一入口 | 低 |

### 阶段 4：长期重构（P1-28 + P2-21/22/23 + P3-1，1-2 月）

| 顺序 | 编号 | 文件 | 修复内容 | 风险 |
|------|------|------|---------|------|
| 34 | P1-28 | core.js | UI 拆分为 ModalManager / ToastManager / NavigationManager / LoadingIndicator | 高 |
| 35 | P2-21 + P2-22 + P2-23 | core.js | THEME_LIBRARY / _mapTheaterByKey / _ERROR_MAPS 外置到 data/ | 中 |
| 36 | P2-7 | core.js | 8 个 parse*Content 抽通用 schema | 中 |
| 37 | P3-1 | core.js | 按领域拆分为 9 个文件 | 极高 |

---

## 附录 A：已确认良好实践（无需修改）

| 检查项 | 结论 | 证据 |
|--------|------|------|
| `escapeHtml` / `escapeAttr` / `escapeJs` 单一来源 | ✅ | 仅 core.js:3989/3998 定义，其他文件直接调用 |
| `AVATAR_COLORS` 单一来源 | ✅ | 仅 phone-ui.js:56 定义一次 |
| `@import` 滥用 | ✅ 无 | Grep `@import` 0 匹配 |
| 内联 `onclick` 在 index.html | ✅ 无 | Grep `onclick=` 0 匹配 |
| 模态 focus trap | ✅ 已实现 | core.js:537/540/570/575/647 |
| `prefers-reduced-motion` | ✅ 已实现 | base.css:1718 |
| `@keyframes pulse` 同名冲突 | ✅ 已修复 | phone-ui.css:517-519 重命名为 `pulse-opacity` |
| 稀有度色板硬编码 | ✅ 已重构 | `#1890ff/#722ed1/#fa8c16/#9e9e9e` 全部迁移到 `--rarity-*` 变量 |
| `#autoSaveDot` 是否死代码 | ✅ 非死 | core.js:4435 `dot.style.animation = 'pulse 0.9s ...'` |
| SVG sprite | ✅ 已用 | index.html:43-183 `<symbol>` + 全文 `<use href="#icon-*">` |
| `aria-live` 公告区 | ✅ 已配置 | index.html:494、1948 |
| `_cachedGuidanceQuest` 跨存档清理 | ✅ 已修复 | game.js:3745-3746 |
| 场景标题三套存储 | ✅ 已统一 | `progress.sceneTitle` / `progress.lastSceneTitle`，`_lastSceneTitle` 仅作 legacy 镜像 |
| `_quickReplyLog` 死字段 | ✅ 已删除 | Grep 零命中 |

---

## 附录 B：审查方法说明

本轮审查采用"5 个并行子代理逐行读取 + 主线程 Grep 交叉验证"模式：

1. **子代理 1**：审查 core.js + utils.js + init.js（~6600 行）
2. **子代理 2**：审查 game.js + systems.js（~5300 行）
3. **子代理 3**：审查 phone-ui.js + tavern-compat.js（~13500 行）
4. **子代理 4**：审查 worldinfo.js + ai-contract/* + state/* + modules/*（~10000 行）
5. **子代理 5**：审查 5 个 CSS 文件 + index.html（~14000 行 CSS + 2846 行 HTML）

每个子代理独立完成：完整 Read 文件 → 列出疑似问题 → Grep 交叉验证（函数名是否被调用、变量是否被读写、CSS 选择器是否被引用）→ 按 P0/P1/P2/P3 分级输出。

主线程在子代理报告基础上做了二次 Grep 验证：
- 跨文件重复模式（escapeHtml、AVATAR_COLORS、empty-state-icon、mergeRelationships 链、_streamMode 全局）
- 关键死代码（_getCachedRegExp、isSaveLocked、extractObj、formatDateTime 等时间函数）
- 关键架构问题（phone-ui.js 直写 gameState 54 处）

所有结论均经 Grep 在 `/workspace/js` 与 `/workspace/index.html` 范围内交叉验证，未修改任何文件。

---

> **报告完毕**。共发现 **128 项问题**（15 P0 + 33 P1 + 48 P2 + 32 P3）。
> 
> **建议执行顺序**：阶段 1（1-2 天）→ 阶段 2（3-5 天）→ 阶段 3（1-2 周）→ 阶段 4（1-2 月）。
> **最高 ROI 修复**：P0-13（撤销 bug，一行修复）+ P2-6（接入 _getCachedRegExp，一行激活缓存并消除死代码）+ P1-18（删除 _pushRelationshipsToGM 双写链，根治关系数据双源）。
