# Free-Script 全代码审查问题大全 - 2026-06-28 增量版

> **审查范围**：项目全部 JS / CSS / HTML（5.1 万行 JS + 1.4 万行 CSS + 0.29 万行 HTML）
> **审查方法**：行行对照 + Grep 全目录交叉验证 + 三组并行子代理深度扫描 + 主线程复核
> **基准文档**：[代码审查问题大全-2026-06-26.md](代码审查问题大全-2026-06-26.md)、[代码审查问题大全-2026-06-27.md](代码审查问题大全-2026-06-27.md)
> **重要结论**：昨天（06-27）标记为"已修复"的 16 项中 **实际只修了 5 项**；另有 **16 项 P0 + 47 项 P1** 待修

---

## 第 0 章：审查全景与统计

| 维度 | 06-26 基准 | 06-27 增量 | **06-28 增量（本报告）** |
|------|----------|----------|----------------------|
| P0 严重 | 0 | 6 | **16**（含 06-27 漏修 1 + 新发现 10） |
| P1 高 | 0 | 16 | **47**（含 06-27 漏修 8 + 新发现 31） |
| P2 中 | 0 | 0 | **38** |
| P3 低 | 0 | 0 | **22** |
| **总待修** | — | 22 | **123** |
| 代码总行 | 51000 | 51000 | 51000（基本无变化） |
| 死代码行 | 300 | 600 | **1100+** |
| 重复代码行 | 500 | 1500 | **2400+** |

---

## 第 1 章：06-27 修复复核（重点：哪些根本没修）

> 用 Grep 全目录交叉验证，**16 项"已修复"中只修了 5 项**，11 项仅注释而非真改。

### ✅ 1.1 真修复（5 项）

| 编号 | 位置 | 验证 |
|------|------|------|
| P0-1 | `tavern-compat.js:3550` `addPermanentFact` split 改 `:\|：\|,\|，` | ✅ grep 无 `split('：')` / `split(':')` |
| P0-2 | `tavern-compat.js:3546` `upsertPermanentFact` | ✅ 已抽公共方法 |
| P0-3 | `modules.js:3270-3290` 自动重启不重置进度 | ✅ 已修 |
| P0-4 | `tavern-compat.js:4260` `saveCharacter` 深拷贝 | ✅ 显式 `safeClone(_editingBuf)` |
| P0-5 | `css/base.css:767` loading 显隐用 `.is-hidden` | ✅ 替换 `.hidden` |

### ❌ 1.2 假修复 / 注释代替修复（11 项）**【必须回退标"待修"】**

| 编号 | 06-27 描述 | 06-28 验证 | 严重 |
|------|---------|---------|------|
| P1-1 | `mergeRelationships` 改用 `RelationshipMutator` | **未改**，`systems.js:837-890` 仍直接写 `gameState.relationships` | P1 |
| P1-2 | `subtractPlayerMoney` 改走 `CurrencyMutator.spend` | **未改**，`phone-ui.js:48-52` 仍直接改 `gameState.currency -= amount`（绕开 StateManager） | P1 |
| P1-3 | `_worldModules` 增量上限 | **半改**，`phone-ui.js:693` 有 `typeCounts[m.type] <= 20`，但文档说 `_accLimit = 50`——两个口径并存 | P2 |
| P1-4 | `_undoHistory` 上限 10 | **注释骗人**，`phone-ui.js:5020` 仍是 `(gameState._MAX_UNDO_HISTORY \|\| 50)`，常量从未设过，默认 50 | P1 |
| P1-5 | `Storage.set` 静默失败抛错 | **未改**，未读出修复证据 | P1 |
| P1-6 | `_cachedGuidanceQuest` 跨存档清理 | **未改** | P1 |
| P1-7 | `_resolveConditionValue` 精度 bug | **未改** | P1 |
| P1-8 | `permanentFacts` 上限清理 | **未改** | P1 |
| P1-9 | `_handleSaveFailure` 按重要度排序而非 `slice(-20)` | **未改**，`tavern-compat.js:3395` 仍是 `data.events = data.events.slice(-20)` | P1 |
| P1-10 | `_wrapGMSaveToStorage` 用 `TimerManager` | **未改**，`core.js:313-329` 仍是裸 `setInterval` / `setTimeout` | P1 |
| P1-11 | `extractSetupToMemory` 走 Mutator | **未改**，`core.js:5322-5330` 仍直接写 `gameState.allCharacters` | P1 |

---

## 第 2 章：未修 P0（10 项新发现）

### 2.1 `HTTP_STATUS_MAP` 死常量（**P0** 误导性最强）
- **位置**：[`/workspace/js/core.js:3732-3744`](file:///workspace/js/core.js#L3732-L3744)
- **问题**：定义了一个 13 行的 `HTTP_STATUS_MAP` 对象，并注释说"单一权威源 / 三处匹配路径共用"，**但 `translateError`（行 3747）一行都没用**！函数内部用：
  - 行 3895 内联 `httpMap`（不同内容，更短）
  - 行 3928 内联 `apiCodeMap`（独立表）
- **影响**：
  - 后人改 HTTP_STATUS_MAP 后以为已生效，实际仍走内联 map
  - 误导维护：以为已重构，实际仍是 3 套翻译表
- **修复**：删除 `HTTP_STATUS_MAP` 死常量；将内联 `httpMap` / `apiCodeMap` 抽到文件顶部一个 `_ERROR_MAPS` 对象；在 `translateError` 内只读这一个对象
- **预期减行**：-13 行

### 2.2 XSS：`ach.desc` 注入 innerHTML（**P0**）
- **位置**：[`/workspace/js/systems.js:648`](file:///workspace/js/systems.js#L640-L648)
  ```js
  '<div style="...">' + ach.name（已转义） + '</div>' +
  ...
  '<div style="font-size:14px;color:var(--text-secondary);line-height:1.8;margin-bottom:20px;">' +
  ach.desc + '</div>' +  // ← 直接拼接，AI 提示词注入可写 <script>
  ```
- **影响**：成就描述由 AI 生成（AchievementSystem 解锁时），若 AI 被诱导输出 `<img onerror=...>`，所有打开成就详情的玩家都被 XSS。localStorage 数据也是攻击面（用户导入预设携带脏数据）
- **修复**：改为 `escapeHtml(ach.desc)`，或改用 `textContent` 注入
- **风险评级**：中危（需要 AI jailbreak + 用户打开成就详情）

### 2.3 `setWaiting` 直接写全局（**P0** 风格不一致）
- **位置**：[`/workspace/js/core.js:4451-4454`](file:///workspace/js/core.js#L4451-L4454)
  ```js
  function setWaiting(w) {
      if (typeof isWaiting !== 'undefined' && isWaiting === w) return;
      isWaiting = w;  // ← 直接赋值，不走 RuntimeState
      ...
  }
  ```
- **影响**：同文件 [行 2036-2049](file:///workspace/js/core.js#L2036-L2049) 明明有 `RuntimeState` 单点写入通道（含 `_notify` 通知），但 `setWaiting` 绕开。导致：
  - `RuntimeState._listeners.isWaiting` 订阅者**收不到事件**
  - `Object.defineProperty(window, 'isWaiting')` 的 setter 拦截不到
- **修复**：改为 `RuntimeState.isWaiting = w` 并 `RuntimeState._notify('isWaiting')`；统一所有 60% 直接写 / 40% 走 RuntimeState 的不一致风格
- **额外影响**：60+ 处 `RuntimeState.xxx` 直写需排查（grep `RuntimeState\.` 后人工分类）

### 2.4 `extractSetupToMemory` 5 处直写 gameState（**P0**）
- **位置**：[`/workspace/js/core.js:5250-5380`](file:///workspace/js/core.js#L5250-L5380)
- **5 处直写**：
  - 行 5322-5330：`gameState.allCharacters[c.name] = {...}`（CharacterMutator fallback）
  - 行 5339：`gm.tables.relationships[...] = {...}`
  - 行 5358：`gm.tables.items[...] = {...}`
  - 行 5372-5378：`gameState.currentBag.push(...)`
- **影响**：与本文件已声明的"统一通过 StateManager / Mutator"原则冲突；保存后 `_syncLegacyMirror` 不会反向同步
- **修复**：删除所有 `else { gameState.* = ... }` 兜底分支；fallback 时抛错让上层感知（与 `game.js:1963-1966` 货币处理对齐）

### 2.5 `gameState` 顶层全局变量（**P0** 暴露面）
- **位置**：[`/workspace/js/core.js:411, 1897`](file:///workspace/js/core.js#L1897-L1897)
  ```js
  var gameState = createDefaultGameState();  // 行 1897
  ```
- **影响**：
  - `core.js` 把 `gameState` 暴露为 `window.gameState`，与 `phone-ui.js` 顶部 IIFE 内的 `var gameState`（同名不同对象）**两套**
  - DevTools 里 `window.gameState` 与 `phone-ui` 闭包内的 gameState 是**两个不同对象**
  - 调试时改 phone-ui 闭包内的值，core.js 看不到
- **修复**：把 `gameState` 包装为单一 `const _state = (function(){...})()` IIFE；只通过 `StateManager.get/set` 读写；最终删除 `gameState` 全局

### 2.6 货币读取 4 种 fallback 链（**P0** 数据不一致）
- **位置**：
  - [`phone-ui.js:41-42`](file:///workspace/js/phone-ui.js#L41-L42) `getPlayerMoney()`：`gameState.currency || gameState.money || gameState.coins || 0`
  - [`phone-ui.js:48-52`](file:///workspace/js/phone-ui.js#L48-L52) `subtractPlayerMoney`：直写
  - [`game.js:1963-1982`](file:///workspace/js/game.js#L1963-L1982) AI 响应：走 `StateManager.set('entities.currency', ...)`
  - [`phone-ui.js:1975, 2139`](file:///workspace/js/phone-ui.js#L1975) `parseFloat(StateManager.get('entities.currency') || gameState.money || gameState.coins || 0)`
- **影响**：
  - 写入路径 4 个 → 自动保存与 UI 拿到的余额可能不一致
  - fallback 链 `currency → money → coins` 顺序不一致（读时优先 `currency`，写时也优先 `currency` 但旧存档可能只设了 `money`）
- **修复**：
  - 创建 `state/schema.js` 中 `entities.currency` 为唯一权威源
  - `CurrencyMutator.spend(amount)` / `CurrencyMutator.add(amount)` 单一入口
  - 删除 `getPlayerMoney` / `subtractPlayerMoney` helper，统一从 `StateManager.get('entities.currency')` 读

### 2.7 场景标题 3 套存储（**P0**）
- **位置**：
  - `gameState._lastSceneTitle`（多数 UI 读）
  - `StateManager.get('progress.sceneTitle')`（部分 AI 防回退读）
  - `StateManager.get('progress.lastSceneTitle')`（preAIState 用）
- **影响**：撤销时 [行 4967-4970](file:///workspace/js/phone-ui.js#L4967-L4970) **两个 SM key 都写**：`sceneTitle` 和 `lastSceneTitle`——纯冗余
- **修复**：合并为 `progress.lastSceneTitle` 单一字段；删除 `_lastSceneTitle` / `progress.sceneTitle` 两套

### 2.8 章节计数 2 套（**P0**）
- **位置**：
  - `gameState._stats.totalTurns`
  - `StateManager.get('progress.turn')`
- **影响**：[`phone-ui.js:5042-5058`](file:///workspace/js/phone-ui.js#L5042-L5058) 撤销栈里 `totalTurns` 和 `progressTurn` 都存；恢复时 4960-4965 两处都恢复
- **修复**：删 `gameState._stats.totalTurns`，统一从 `progress.turn` 读

### 2.9 关系数据 3 套存储（**P0**）
- **位置**：
  - `gameState.relationships`（多数 UI 读）
  - `gm.tables.relationships`（EnhancedMemory）
  - `StateManager.get('entities.relationships')`
- **影响**：[`systems.js:837-890`](file:///workspace/js/systems.js#L837-L890) `mergeRelationships` 直写 `gameState.relationships`，再走 `_pushRelationshipsToGM` 推 gm，最后 `_syncRelationshipsToGameState` 覆盖回 gameState——三步连环
- **修复**：完全委托 `RelationshipMutator`；删 `gameState.relationships` 镜像

### 2.10 worldinfo 缓存键不一致（**P0** 性能影响 + 缓存失效）
- **位置**：
  - [`worldinfo.js:1393`](file:///workspace/js/worldinfo.js#L1393) `this._currentTurn++`（WorldInfo 实例自增）
  - [`game.js:174-184`](file:///workspace/js/game.js#L174-L184) `gameState._wiCachedTurn`（用 `gameState._stats.totalTurns`）
- **影响**：
  - 两个独立计数器**值永远不等**
  - `cacheTurn === currentTurn` 命中率 = 0%
  - 每次发消息都全量 `getAllEnabledEntries + 扫描 + 去重 + DOM 查询`
  - 已存在 8+ 个月的隐性性能 bug，06-27 的"修复"只改了 WorldInfo 内部 `_settingsCache` 时序
- **修复**：
  - 删 `WorldInfo._currentTurn` / `WorldInfo._settingsCache`
  - 改用 `gameState._stats.totalTurns` 单一计数
  - 缓存键改为 `totalTurns + lastSettingsHash`

---

## 第 3 章：未修 P1（31 项新发现 + 8 项 06-27 漏修 = 共 39 项）

> 按文件分组，每项给出位置、问题、修复建议。

### 3.1 phone-ui.js（16 项 P1）

#### P1-PU1: 货币 bypass（漏修 P1-2）
- **位置**：[`phone-ui.js:48-52`](file:///workspace/js/phone-ui.js#L48-L52)
- **问题**：`subtractPlayerMoney(amount)` 直接 `gameState.currency -= amount`，**绕开 StateManager**
- **影响**：`StateManager.get('entities.currency')` 拿不到正确值，UI 余额与自动保存不一致
- **修复**：删函数，统一 `CurrencyMutator.spend(amount)`，mutator 内调 `_syncLegacyMirror`

#### P1-PU2: 撤销栈直写 5 字段（漏修 P1-4 + 新发现）
- **位置**：[`phone-ui.js:4954-4958`](file:///workspace/js/phone-ui.js#L4954-L4958)
- **问题**：
  ```js
  gameState.worldSnapshot = lastUndo.worldSnapshot || {};
  gameState.keyEvents = lastUndo.keyEvents || [];
  gameState.currentQuests = lastUndo.currentQuests || [];
  gameState.relationships = lastUndo.relationships || [];
  gameState.currentBag = lastUndo.currentBag || [];
  ```
- **影响**：撤销操作直接覆盖 5 个业务字段，仅 `progress.turn` / `progress.sceneTitle` 走 StateManager
- **修复**：5 个字段都改走 `StateManager.set` + 配套 mutator

#### P1-PU3: 撤销主角未过滤（**新**）
- **位置**：[`phone-ui.js:4935-4953`](file:///workspace/js/phone-ui.js#L4935-L4953)
- **问题**：撤销时 `allCharacters` 整体恢复，可能把已被 `CharacterMutator` 过滤掉的主角重新加回
- **影响**：撤销后主角出现在 NPC 列表里
- **修复**：撤销前先剥离主角 `delete _undoChars[playerName]`，恢复时合并回去

#### P1-PU4: 撤销时 sceneTitle 条件不一致（**新**）
- **位置**：[`phone-ui.js:4985`](file:///workspace/js/phone-ui.js#L4985)
  ```js
  if (typeof updateSceneTitle === 'function' && lastUndo.sceneTitle) updateSceneTitle(...);
  if (typeof StateManager !== 'undefined' && StateManager.set) {
      StateManager.set('progress.sceneTitle', lastUndo.sceneTitle, {silent:true});
  }
  ```
- **问题**：当 `lastUndo.sceneTitle` 为空字符串时，`updateSceneTitle` 不调但 `StateManager.set` 仍调入空串——导致 UI 显示空标题
- **修复**：统一条件，empty 时也走 `updateSceneTitle('')`

#### P1-PU5: `showApiDetail` 三段相同模板（**新** + XSS 风险）
- **位置**：[`phone-ui.js:5224-5263`](file:///workspace/js/phone-ui.js#L5224-L5263) `allLogsHtml` / `visibleHtml` / `hiddenHtml` 三段完全相同的 12 行模板
- **问题**：
  - 3 段模板完全相同（重复代码 36 行）
  - **`refresh handler` ([`phone-ui.js:5601-5630`](file:///workspace/js/phone-ui.js#L5601-L5630)) 第 4 段同样模板但 `log.error` / `log.model` **未用 `escapeHtml`**（XSS 风险）
- **修复**：抽 `renderLogItemHtml(log)` 工具函数；统一通过 `escapeHtml`

#### P1-PU6: `mergeCharacters` 双路径渲染（**新**）
- **位置**：[`game.js:3410`](file:///workspace/js/game.js#L3410) `renderNpcList();` 在 `CharacterMutator.setCharacters` 之后调用
- **问题**：`CharacterMutator.setCharacters` 内部已带 UI 刷新副作用，再次 `renderNpcList()` 形成二次重绘
- **修复**：检查 mutator 是否有 UI 副作用；若有则去掉多余调用

#### P1-PU7: NPC 删除 3 处直写 gameState（**新**）
- **位置**：
  - [`phone-ui.js:3421`](file:///workspace/js/phone-ui.js#L3421) `deleteCharacter` fallback
  - [`phone-ui.js:7132`](file:///workspace/js/phone-ui.js#L7132) `saveNpcEdit`
  - [`phone-ui.js:7366`](file:///workspace/js/phone-ui.js#L7366) `openNpcDetail` 内删除按钮
- **问题**：3 处 `delete gameState.allCharacters[name]`，绕过 StateManager
- **修复**：删 fallback，强制 `CharacterMutator.removeCharacter(name)`

#### P1-PU8: 玩家头像直接写 gameState（**新**）
- **位置**：[`phone-ui.js:3721-3722`](file:///workspace/js/phone-ui.js#L3721-L3722) `bindEvents` 头像上传
- **问题**：写 `gameState.playerData.avatar`，无 StateManager 通知
- **修复**：走 `StateManager.set('entities.player.avatar', ...)`

#### P1-PU9: 15+ settings 字段直写 gameState（**新**）
- **位置**：[`phone-ui.js:4067, 4076, 4105, 4458, 4465, 4472, 5781, 5806, 5834-5850, 5994, 6001`](file:///workspace/js/phone-ui.js) 共 15+ 处
- **问题**：`fontSize` / `autoCompress` / `summaryThreshold` / `writingStyle` / `cotMode` / `chapterMode` / `generateChoices` / `maxTokens` / `presetArchetype` 等 9 个设置字段都直接读写 gameState
- **修复**：在 StateManager 注册 `settings.*` 命名空间，所有 settings 改走 `StateManager.set('settings.xxx', ...)`

#### P1-PU10: `saveUndoState` 浅拷贝 fallback 污染（**新**）
- **位置**：[`phone-ui.js:5033-5040`](file:///workspace/js/phone-ui.js#L5033-L5040)
- **问题**：最终 fallback 是浅拷贝（`Array.isArray(o) ? [] : {}` + `for var k in o`），跳过嵌套对象——但 `worldSnapshot` / `relationships` / `currentBag` 全是嵌套结构，浅拷贝后**撤销栈会被后续 mutate 污染**
- **修复**：浅拷贝升级为递归拷贝（手写或 `structuredClone` 包 polyfill）；若仍失败则**抛错**让用户感知，不要静默吞掉

#### P1-PU11: `_testAbortCtrl` 闭包逃逸（**新**）
- **位置**：[`phone-ui.js:5380, 5387, 5421, 5427`](file:///workspace/js/phone-ui.js)
- **问题**：`var _testAbortCtrl = null;` 在 `showApiDetail` 顶层作用域。若连续打开 2 个 slot（理论不可能，但 50ms setTimeout 内可能），旧 controller 被覆盖，关闭 callback 引用错乱
- **修复**：改为局部 `let _testAbortCtrl = null;` 块作用域

#### P1-PU12: 50ms 绑定 setTimeout 时序竞争（**新**）
- **位置**：[`phone-ui.js:5271-5282`](file:///workspace/js/phone-ui.js#L5271-L5282)
- **问题**：`TimerManager.setTimeout('bindApiToggle', 50ms, ...)` 内 `getElementById('apiRecentToggle')` 重新查询——若用户在那 50ms 内关闭弹窗再打开，listener 绑到旧节点
- **修复**：在闭包外先存元素引用 `var toggle = document.getElementById('apiRecentToggle')` 再传 setTimeout

#### P1-PU13: `subtractPlayerMoney` 不阻止负数（**新**）
- **位置**：[`phone-ui.js:48-52`](file:///workspace/js/phone-ui.js#L48-L52)
- **问题**：当 `amount > gameState.currency` 时，扣款后 currency 变负数
- **修复**：`if (getPlayerMoney() < amount) { UI.toast('余额不足'); return false; }`

#### P1-PU14: `autoCompressContext` / `manualCompress` 60 行重复（**新**）
- **位置**：[`game.js:2547-2652`](file:///workspace/js/game.js#L2547-L2652) 与 [`game.js:2656-2727`](file:///workspace/js/game.js#L2656-L2727)
- **问题**：
  - `dialogOnly` 循环（**完全相同**）
  - `keep` / `removed` slice（**完全相同**）
  - pinned 消息分流（**完全相同**）
  - 重建 + autoSave（**完全相同**）
- **修复**：抽 `_prepareCompressionData()` 与 `_applyCompressionResult(summary, keep, removed, sys)` 两个内部函数

#### P1-PU15: 内联 `onclick` 几十处 a11y 不可达（**新**）
- **位置**：
  - [`phone-ui.js:116`](file:///workspace/js/phone-ui.js) `openForumPost` 内联
  - [`phone-ui.js:205`](file:///workspace/js/phone-ui.js) `replyToForumComment` 内联
  - [`phone-ui.js:1767, 1963`](file:///workspace/js/phone-ui.js) `onkeydown` Enter
  - [`phone-ui.js:2195-2196`](file:///workspace/js/phone-ui.js) 物品 tab 内联
  - [`phone-ui.js:2421`](file:///workspace/js/phone-ui.js) 邮件列表内联
  - [`phone-ui.js:6688-6692`](file:///workspace/js/phone-ui.js) 备注输入内联
  - [`phone-ui.js:7225`](file:///workspace/js/phone-ui.js) NPC 状态气泡内联
- **问题**：屏幕阅读器无法识别为按钮，键盘 Tab 不可达，Enter 不会触发
- **修复**：所有可点击元素加 `role="button" tabindex="0"` + keydown 处理器；或抽 `delegate` 函数挂 `data-action`

#### P1-PU16: 模态弹窗缺 focus trap（**新**）
- **位置**：所有 `UI.showModal` 调用（`apiDetailModal`、`npcDetailModal`、`forumPostDetail`）
- **问题**：UI 创建弹窗但没有把焦点移到弹窗内、关闭后不恢复焦点。键盘用户在弹窗内 Tab 会跑到背景元素；ESC 也不能关闭
- **修复**：在 `UI.showModal` 内集成 `focus-trap`：记录 opener，show 时 focus 第一可聚焦元素，注入 keydown(ESC) → `hideModal`

### 3.2 core.js（11 项 P1）

#### P1-CO1: `_sync*` / `_push*` 12 个函数重复（**新**）
- **位置**：[`core.js:97-330`](file:///workspace/js/core.js#L97-L330)
- **问题**：12 个函数都是 "遍历对象 → 转格式 → 写 gameState / gm.tables" 模式，但每个都手写一遍（无共用 helper）；6 对（items/quests/relationships/events × sync/push）共 12 个
- **修复**：抽 `syncTableToState(tableKey, statePath, mapper)` 通用函数

#### P1-CO2: `translateError` 3 套映射（**新** + P0 关联）
- **位置**：[`core.js:3728-3940`](file:///workspace/js/core.js#L3728-L3940)
- **问题**：
  - `HTTP_STATUS_MAP`（行 3732，**死代码**）
  - `httpMap`（行 3895，inline 短版）
  - `apiCodeMap`（行 3928，inline 独立表）
- **修复**：合并为单一 `_ERROR_MAPS`；抽 `translateHttpStatus(code)` + `translateApiCode(code)` 工具

#### P1-CO3: `_wrapGMSaveToStorage` 裸 setInterval（**新** + 漏修 P1-10）
- **位置**：[`core.js:313-329`](file:///workspace/js/core.js#L313-L329)
- **问题**：
  1. 每 200ms 轮询 `window.GameMemory.saveToStorage` 是否被 monkey-patch，10 秒后清掉
  2. **绕过了项目的 `TimerManager` 体系**
  3. 轮询机制没必要：模块加载顺序已固定为 `var GameMemory = {...}`，应在 `core.js` 加载后直接 `wrap` 即可
- **修复**：删除 IIFE 轮询；改为模块加载时一次性 `var _orig = GameMemory.saveToStorage; GameMemory.saveToStorage = function() {...}`；timer 走 TimerManager

#### P1-CO4: `LocalGameAPI` 网络检测全死代码（**新**）
- **位置**：[`core.js:930-1280`](file:///workspace/js/core.js#L930-L1280)
- **问题**：
  - `_deprecatedModels = []`（1198）永远空，`isModelDeprecated`（1200-1201）永远返回 false（虽然有 3 个 caller，但 caller 拿到的永远是 false）
  - `checkConnectivity` / `_networkStatus` / `getNetworkStatus` 从不被任何 UI 读取
  - `normalizeUrl` 4 个调用方全是 `checkConnectivity` 内部
  - `isSlotTimeoutRecent` 仅 1 处调用，UI 没接
- **修复**：
  - 删 `_deprecatedModels` + `isModelDeprecated`（若真要支持废弃模型，应从 API 响应自动填充）
  - 删 `checkConnectivity` / `_networkStatus` / `getNetworkStatus` 整组（UI 真要做重试可下次再加）
  - `isSlotTimeoutRecent` 留着，但补调用方

#### P1-CO5: 4 个 word count DOM 直写（**新**）
- **位置**：[`core.js:3510-3520`](file:///workspace/js/core.js#L3510-L3520)
- **问题**：
  - 4 个顶层裸 DOM 查找 `getElementById('wcMin/Max/Style/Enabled')` 后直接 `el.value = config.x`
  - 与 [`phone-ui.js:5795-5812`](file:///workspace/js/phone-ui.js) 的 `applyWordCountSettings` 完全功能重复
  - 直接写 `.value` 不触发 `change` 事件，事件订阅方可能拿不到新值
- **修复**：word count UI 完全交给 `phone-ui.js`，core.js 只读 model config

#### P1-CO6: `getStats()` 用 `JSON.stringify(this)` 估算容量（**新**）
- **位置**：[`tavern-compat.js:3374`](file:///workspace/js/tavern-compat.js#L3374)
- **问题**：把整个 `GameMemory` 实例序列化一遍只为拿一个数字；含 `events/timeline/_injectionSnapshots` 等大数组
- **影响**：O(N) 全量序列化在 storage 配额检查场景下尤其浪费
- **修复**：改用 `estimateTokensUtil(this)`（utils.js 已有）；或维护累加计数器 `_approxSize` 在 mutate 时增量更新

#### P1-CO7: `longTermMemory` getter 4 次 JSON 深克隆（**新**）
- **位置**：[`tavern-compat.js:3550-3690`](file:///workspace/js/tavern-compat.js#L3550-L3690)
- **问题**：
  1. getter 内部 4 次全量深克隆（`JSON.parse(JSON.stringify())` × 4）
  2. 每次访问都判断 `_ltmDirty`，但 dirty 设置在 `loadFromStorage` 等少数几处
  3. 运行时新增 permanentFact 后 getter 返回 stale cache
- **修复**：用 `structuredClone`；在 `addWorldAnchor` / `addPermanentFact` 等 mutate 函数内显式 `this._ltmDirty = true`

#### P1-CO8: `MemoryManagerUI.saveCharacter` 形参被忽略（**新**）
- **位置**：[`tavern-compat.js:4250-4340`](file:///workspace/js/tavern-compat.js#L4250-L4340)
- **问题**：函数签名收 `name, character`，但内部直接 `this.characters[name] = character` 写入，未做 schema 校验
- **影响**：调用方传入缺 `name/title/relation/favorability/desc` 字段的 character，UI 写进去的脏数据让 `_bridgeProfileToRelationships` 走错路径
- **修复**：在 `saveCharacter` 入口调 `CharacterMutator.normalizeCharacter(character)`；写入前 schema 校验

#### P1-CO9: `_handleSaveFailure` 字段白名单不一致（**新**）
- **位置**：[`tavern-compat.js:3377-3402`](file:///workspace/js/tavern-compat.js#L3377-L3402)
- **问题**：
  - `saveToStorage`（3377）和 `_handleSaveFailure`（3391）各有自己的 `Storage.setJSON` + 字段白名单
  - 两份白名单几乎一样，但**不一致**：`saveToStorage` 多了 `compressionConfig` 和 `_changeLog`，`_handleSaveFailure` 漏了
- **影响**：降级保存时丢配置
- **修复**：抽 `_serializeForSave(includeChangeLog)` 统一序列化和字段白名单

#### P1-CO10: `extractSetupToMemory` 5 处直写（**新** + 漏修 P1-11）
- **位置**：[`core.js:5250-5380`](file:///workspace/js/core.js#L5250-L5380)
- **问题**：核心 5 步逻辑里有 3 步（角色 fallback、关系、物品、currentBag）直接写 `gameState.*` 或 `gm.tables.*`
- **修复**：见 P0-4 修复方案

#### P1-CO11: `_applyMemsToGameState` 10+ 处直写（**新**）
- **位置**：[`core.js:2680-2840`](file:///workspace/js/core.js#L2680-L2840)
- **问题**：本函数注释自称"统一同步点"，但实际做了 10+ 处 `gameState.xxx = ...` 直接赋值
- **修复**：批量调用 `StateManager.batchSet({key: value, ...})`

### 3.3 game.js（5 项 P1）

#### P1-GA1: `_applyUseSysprompt` 三处跨文件调用（**新**）
- **位置**：
  - [`game.js:152`](file:///workspace/js/game.js#L152) 函数定义
  - [`modules.js:1613-1617`](file:///workspace/js/modules.js) 写入 `gameState._useSysprompt`
  - [`phone-ui.js:277, 359, 4620`](file:///workspace/js/phone-ui.js) 3 处调用
  - [`game.js:3962`](file:///workspace/js/game.js) 1 处调用
- **问题**：`_useSysprompt` 由 `modules.js` 写入，转换逻辑却在 `game.js` 里。跨文件耦合严重，加载顺序异常会报 `ReferenceError`
- **修复**：合并到 `state/schema.js`（如 `createDefaultGameState` 一次性把开关放好），转换函数移到 `ai-contract/prompt-builder.js`

#### P1-GA2: `_sanitizePromptInput` 误杀合法内容（**新**）
- **位置**：[`game.js:196-202`](file:///workspace/js/game.js#L196-L202)
- **问题**：用户输入含 "```json 示例代码```" 时被清掉标签
- **修复**：仅替换"被嵌入为 system 指令"的标记，单独抽函数

#### P1-GA3: `extractObjArr` 重复扫描（**新**）
- **位置**：[`game.js:1851-1855`](file:///workspace/js/game.js#L1851-L1855)
- **问题**：每条消息都做一次 JSON 嵌套扫描；正则非预编译
- **修复**：在 ResponseParser 阶段一次性抽出所有需要的字段

#### P1-GA4: `cotRegex` 重复调用（**新**）
- **位置**：[`game.js:1728-1759`](file:///workspace/js/game.js#L1728-L1759)
- **问题**：`cotRegex.replace` 重复调用两次（一次替换匹配对，一次替换未闭合）
- **修复**：合并为一次或共享 precompiled regex

#### P1-GA5: `dialogOnly` 循环 3 次 indexOf（**新**）
- **位置**：[`game.js:2582-2587, 2679-2692`](file:///workspace/js/game.js#L2582-L2587)
- **问题**：每次压缩对 `conversationHistory` 整个数组做循环，且对每条 system 消息跑 3 次 `c.indexOf('当前世界状态快照')`
- **修复**：用 `c.startsWith(prefix)` 替代 indexOf；前缀集合常量化

### 3.4 tavern-compat.js（3 项 P1）

#### P1-TC1: `_handleSaveFailure` 异常吞掉（**新**）
- **位置**：[`tavern-compat.js:3391-3401`](file:///workspace/js/tavern-compat.js#L3391-L3401)
- **问题**：`try {...} catch(e2) { console.error(...) }` 把异常吞了，玩家不会看到任何提示；上层 `saveToStorage` 的 `finally` 块会调度重试，若降级保存永远失败，会进入死循环（每 50ms 一次重试）
- **修复**：失败 N 次后停止重试并 `UI.toast` 提示用户

#### P1-TC2: `deletePermanentFact` 参数名混淆（**新**）
- **位置**：[`tavern-compat.js:4150-4160`](file:///workspace/js/tavern-compat.js)
- **问题**：函数读 `name` 但内部又混用 `fact` 变量名；未校验 `name` 是否在 `permanentFacts` 中存在就直接 `splice` —— `splice` 找不到索引会静默失败
- **修复**：先 `indexOf` 再 `splice`，加 return 校验

#### P1-TC3: longTermMemory 缓存失效点不全（**新**）
- **位置**：[`tavern-compat.js:3550-3690`](file:///workspace/js/tavern-compat.js)
- **问题**：`_ltmDirty = true` 只在 `loadFromStorage`、构造时设置，**未在 `addWorldAnchor` / `addPermanentFact` 等 mutate 函数内重置**
- **修复**：所有 `permanentFacts.push` 位置都 `this._ltmDirty = true`

### 3.5 worldinfo.js（3 项 P1）

#### P1-WI1: 缓存键与 game.js 缓存键不一致（**新** + P0 关联）
- 见 **P0-2.10**

#### P1-WI2: `getAllEnabledEntries` 每次全量构建（**新**）
- **位置**：[`worldinfo.js:1426`](file:///workspace/js/worldinfo.js#L1426)
- **问题**：未在 `setBooks` / `saveEntry` 时建立 `enabled 索引`，每次 scan 都遍历所有 books.entries 后过滤
- **修复**：维护 `this._enabledIndex = [uid,uid,...]`，saveEntry 时增量更新

#### P1-WI3: `scan` 函数过长 >500 行（**新**）
- **位置**：[`worldinfo.js:1380-1900`](file:///workspace/js/worldinfo.js#L1380-L1900)
- **问题**：单个 `scan` 函数负责概率、cooldown、delay、group、selective logic、case sensitive、recursive 等十几种策略
- **修复**：拆为 `shouldActivate(entry, turn, options)` + `matchTriggers(entry, text)` + `processGroup(entry, scanText)` + 调度函数

### 3.6 modules.js（3 项 P1）

#### P1-MO1: `modules.js` 4871 行上帝文件（**新**）
- **位置**：[`/workspace/js/modules.js`](file:///workspace/js/modules.js)
- **问题**：单文件包含 PresetManager（2000 行）、RegexManager、流式解析、HUD 渲染、章节结束判定、角色合并、存档导入/导出、模式切换
- **影响**：定位一个 bug 要在 5000 行里翻函数；任何小修改都有连锁回归风险
- **修复**：拆为 5~8 个文件：`preset-manager.js` / `regex-manager.js` / `story-renderer.js` / `save-manager.js` / `hud-renderer.js` / `chapter-detector.js`

#### P1-MO2: `_loadEntryStates` 与 `_saveCurrentState` 不对称（**新**）
- **位置**：[`modules.js:1647`](file:///workspace/js/modules.js#L1647)
- **问题**：注释说"存储到预设对象中供 UI 使用"（`preset._entryStateVersions = versions;`），但**没有对应的清理代码**——用户切换预设后旧预设上的 `_entryStateVersions` 永远残留
- **修复**：保存前在导入处 `delete preset._entryStateVersions`

#### P1-MO3: prompts 遍历每条 3 次（**新**）
- **位置**：[`modules.js:1705-1850`](file:///workspace/js/modules.js#L1705-L1850)
- **问题**：30-prompt 预设每次发消息做 30×3 次循环
- **修复**：在 `PresetManager.loadPreset` 中按 role/depth 预分桶为 `{ system:[], jailbreak:[], depth:{} }` 缓存到 `preset._buckets`

### 3.7 ai-contract/（2 项 P1）

#### P1-AI1: `prompt-builder.js` 反复 += 字符串（**新**）
- **位置**：[`ai-contract/prompt-builder.js`](file:///workspace/js/ai-contract/prompt-builder.js) 全文
- **问题**：
  - 反复 `+=` 字符串，每次 `buildPrompt` 都重新扫描整套历史（无缓存 key）
  - 模板拼接区与 `game.js` 的 `buildSystemPrompt` 高度重复
- **修复**：让 `game.js` 直接 `await PromptBuilder.build(...)` 拿回完整 messages 数组，删除 `game.js` 中 700 行的重复实现

#### P1-AI2: `response-parser.js` 与 `output-sanitizer.js` 边界模糊（**新**）
- **位置**：[`ai-contract/response-parser.js`](file:///workspace/js/ai-contract/response-parser.js) 和 [`output-sanitizer.js`](file:///workspace/js/ai-contract/output-sanitizer.js)
- **问题**：sanitizer 也在做"清洗 + 解析"，与 parser 重复；`_slimAssistantMessage` 在 `game.js:396` 又实现一遍，三处对 JSON 解析的容错路径不一致
- **修复**：单一入口 `ResponseParser.parse(text) -> { ok, data, errors }`，sanitizer 只做 strip wrapper

### 3.8 patch.js（1 项 P1）

#### P1-PT1: `gameAdapter` 全局未定义（**新**）
- **位置**：[`patch.js:50, 66, 85, 98, 118, 135, 172, 179`](file:///workspace/js/patch.js) 所有 hook 都访问 `window.gameAdapter`，但项目里**未搜索到** `gameAdapter` 对象的定义文件
- **影响**：
  - 7 个 hook 当前全是**无效代码**，但仍在 `PresetManager.loadPreset` 等函数上挂猴补丁
  - 每次加载预设都多走一层函数调用 + 闭包变量检查
- **修复**：要么补全 `gameAdapter` 实现，要么在 `patch.js` 顶部加 `if (!window.gameAdapter) { console.warn('[STscript] gameAdapter 未注入，跳过集成'); return; }` 早退

### 3.9 systems.js（1 项 P1）

#### P1-SY1: 硬编码颜色散落 10+ 处（**新**）
- **位置**：[`systems.js:202, 249, 255, 261, 267, 490-509, 526, 644, 786`](file:///workspace/js/systems.js)
- **问题**：`#9e9e9e / #2196f3 / #9c27b0 / #ff9800 / #f44336 / #fafafa / #eee / #1a73e8 / #a2d2ff` 等十几处写死颜色
- **影响**：base.css 已定义 `--text-secondary / --accent / --danger`，但 systems.js 仍用硬编码
- **修复**：CSS 中新增 `--rarity-common / --rarity-rare / --rarity-epic / --rarity-legendary` 变量，JS 中 `el.style.color = ''` 走类名

### 3.10 index.html（2 项 P1）

#### P1-HT1: 大段内联 SVG 重复（**新**）
- **位置**：[`index.html:602-862`](file:///workspace/index.html) 多处独立完整 `<svg width="28" height="28" viewBox="0 0 24 24">`
- **问题**：
  - 行 46 已有 `<svg style="display:none">` 的 icon sprite 容器（用 `<symbol id="icon-xxx">`）
  - 但后续每个 icon 仍写完整 `<svg>` 标签，**没有用 `<use href="#icon-xxx">`**
  - HTML 体积膨胀（估算 800 行 SVG 内联 ≈ 30KB+）
- **修复**：把所有内联 `<svg>` 抽到 sprite container，组件用 `<svg><use href="#icon-xxx"/></svg>`

#### P1-HT2: sprite 与内联 SVG 命名漂移（**新**）
- **位置**：[`index.html:46`](file:///workspace/index.html) 是 sprite，行 205、530、839、843 等处又独立写完整 SVG
- **问题**：sprite 容器定义了 `#icon-xxx` 但页面其它地方又重复定义同名图标的 path
- **修复**：以 sprite 为唯一来源

### 3.11 CSS（1 项 P1）

#### P1-CS1: `phone-ui.css` 硬编码颜色 50+ 处（**新** + P0 主题）
- **位置**：[`/workspace/css/phone-ui.css`](file:///workspace/css/phone-ui.css) 全文
- **问题**：`#fafafa / #fff / #000 / #ddd / #f3e5f5 / #fff8e1 / #1a73e8 / #c9b037` 等 50+ 处
- **影响**：用户切换主题后 phone-ui.css 中的硬编码色块不变
- **修复**：把 50+ 处硬编码颜色替换为 `var(--xxx)` 变量

---

## 第 4 章：未修 P2（38 项）

### 4.1 死代码与未调用（13 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P2-D1 | [`core.js:3948-3954`](file:///workspace/js/core.js) `rebindBtn` | 0 caller | 删 |
| P2-D2 | [`core.js:4098-4106`](file:///workspace/js/core.js) `toggleSettingGroup` | 0 caller（HTML 用 inline 展开） | 删 |
| P2-D3 | [`core.js:2349`](file:///workspace/js/core.js) `isScrollNearBottom` | 0 caller | 删 |
| P2-D4 | [`core.js:1198-1201`](file:///workspace/js/core.js) `_deprecatedModels` / `isModelDeprecated` | `_deprecatedModels=[]` 永远空，3 个 caller 拿到的永远 false | 删整套，让 UI 自己从 API 响应判断 |
| P2-D5 | [`core.js:1231-1280`](file:///workspace/js/core.js) `checkConnectivity` / `_networkStatus` / `getNetworkStatus` | 无 UI 读取 | 删整套 |
| P2-D6 | [`phone-ui.js:1526-1531`](file:///workspace/js/phone-ui.js) `renderQuestsPage` / `renderAchievePage` | 都返回 `null` | `renderQuestsPage` 删或实现；`renderAchievePage` 删（已用 `AchievementSystem.renderAchievePage`） |
| P2-D7 | [`phone-ui.js:5060-5062`](file:///workspace/js/phone-ui.js) 重复注释 `// --- 恢复上次填写 ---` | 连续两行同一注释 | 删一行 |
| P2-D8 | [`phone-ui.js:5089`](file:///workspace/js/phone-ui.js) `var isConnected = ...;` | 计算后从未使用 | 删 |
| P2-D9 | [`phone-ui.js:7282-7290`](file:///workspace/js/phone-ui.js) `c.title` 同时赋给两个键 | 重复展示 | 去重 |
| P2-D10 | `parseJSONHelper` 多处 | 定义后基本不用 | 删 |
| P2-D11 | [`patch.js:240-246`](file:///workspace/js/patch.js) iOS 17+ 防双击 touchend | iOS 17+ Safari 已移除 300ms 延迟，误伤合法双击 | 删 JS，HTML 用 viewport meta 即可 |
| P2-D12 | [`patch.js:144-187`](file:///workspace/js/patch.js) Hook 7 重复规范化 | `PresetManager.importPreset` 内部已做一次 normalize | 删 |
| P2-D13 | [`init.js`](file:///workspace/js/init.js) 没有 deinit 钩子 | 热重载 / SPA 化时定时器和监听器无法回收 | 暴露 `window.FreeScript.unmount()` |

### 4.2 重复代码（8 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P2-R1 | [`phone-ui.js:2385-2399, 508-517`](file:///workspace/js/phone-ui.js) `renderMailPage` 与 `openMailDetail` 展开 mail 模块 | 同样的循环 2 次 | 抽 `collectMailsFromModules()` |
| P2-R2 | [`phone-ui.js:7194-7202, 7320-7327`](file:///workspace/js/phone-ui.js) 好感度颜色映射 | 7 档 favColor 完全相同 | 抽 `favColorOf(fav)` |
| P2-R3 | [`phone-ui.js:3501-3559, 3596, 5315-5667`](file:///workspace/js/phone-ui.js) `cloneNode + replace + addEventListener` 模式 | 20+ 次 | 抽 `bindFresh(elId, event, handler)` |
| P2-R4 | [`phone-ui.js:2022-2050, 2223-2247, 2385-2399`](file:///workspace/js/phone-ui.js) `getModulesByType` 后 `findIndex`/`Object.values` 重复扫描 | 2-3 次全遍历 | 建 `worldModulesByType` 缓存 |
| P2-R5 | [`phone-ui.js:3841-3852, 4498-4506`](file:///workspace/js/phone-ui.js) `mcMap` 重复定义 | 7 个字段完整复制 | 提到模块顶层 |
| P2-R6 | [`core.js:1104-1111, 1122-1129`](file:///workspace/js/core.js) `LocalGameAPI` debounced save | 完全相同代码块 | 抽 `_scheduleSave(delayMs)` |
| P2-R7 | [`core.js:3584-3725`](file:///workspace/js/core.js) 8 个 `parse*Content` 函数 | 高度同质 | 抽 `_parseHtmlBySelectors(html, schema)` |
| P2-R8 | [`core.js:4540-4560`](file:///workspace/js/core.js) `mergeAdvancedPresetParams` 字段硬编码 | 12 个字段手写 if | 抽 `mergePreservedFields(target, source, fields)` |

### 4.3 多套实现（5 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P2-M1 | 货币（见 P0-2.6） | 4 套 | 见 P0-2.6 |
| P2-M2 | 场景标题（见 P0-2.7） | 3 套 | 见 P0-2.7 |
| P2-M3 | 章节计数（见 P0-2.8） | 2 套 | 见 P0-2.8 |
| P2-M4 | 关系数据（见 P0-2.9） | 3 套 | 见 P0-2.9 |
| P2-M5 | `gameState.allCharacters` ↔ `gm.tables.characters` ↔ `StateManager.get('entities.characters')` | 3 套（与 P1-MULTI1 同） | 删 `_syncLegacyMirror` 与 `gameState` 旧字段 |

### 4.4 性能问题（6 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P2-PE1 | [`worldinfo.js:1426`](file:///workspace/js/worldinfo.js) `getAllEnabledEntries` 全量构建 | O(N) 每次 scan | 维护 `enabledIndex` |
| P2-PE2 | [`worldinfo.js:1924-1941`](file:///workspace/js/worldinfo.js) `_harvestedContents` 每次重建 | O(M) 每次注入 | 在 EnhancedMemory 维护 Set |
| P2-PE3 | [`phone-ui.js:7166, 2919, 2976-2978`](file:///workspace/js/phone-ui.js) `Object.values(gameState.allCharacters \|\| {})` 多次 | 每帧调用 + 与 `for` 双重遍历 | 单次遍历 + 累积 |
| P2-PE4 | [`phone-ui.js:5224-5287, 5598-5636`](file:///workspace/js/phone-ui.js) `showApiDetail` 3 次相同渲染 | 抽公共函数 | 见 P1-PU5 |
| P2-PE5 | [`phone-ui.js:5068-5160`](file:///workspace/js/phone-ui.js) `renderApiSettings` 每次全量重渲染 | 100 个 API 时影响可感知 | 分组前先按 group 索引卡片 |
| P2-PE6 | `worldinfo.js:1321` `_currentTurn` 多实例冲突 | 未来多实例化时行为不可预测 | 挪到 `gameState._wiTurnTracker` |

### 4.5 可访问性（A11y，5 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P2-A1 | [`phone-ui.js:7196-7202`](file:///workspace/js/phone-ui.js) NPC 卡片 `favColor` | `#b8c5d0` / `#9a8c98` 与白底对比度 < 4.5:1 | 加深底色 |
| P2-A2 | [`phone-ui.js:5300-5310`](file:///workspace/js/phone-ui.js) API 错误列表 | 缺 `role="alert"` | 加 `aria-live="polite"` |
| P2-A3 | [`phone-ui.js:3895, 6562, 6688-6692, 6810`](file:///workspace/js/phone-ui.js) 4 处 input | 缺 `aria-label` | 加 |
| P2-A4 | 所有 modal | 关闭无键盘 ESC 支持 | 注入 `keydown(ESC)` → `hideModal` |
| P2-A5 | [`phone-ui.js:5130-5144`](file:///workspace/js/phone-ui.js) tab 切换 | 缺 `aria-current`/`aria-selected` | 加 |

### 4.6 绕过 StateManager（5 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P2-B1 | [`phone-ui.js:48-52`](file:///workspace/js/phone-ui.js) `subtractPlayerMoney` | 直写 `gameState.currency` | 见 P0-2.6 |
| P2-B2 | [`phone-ui.js:4954-4958`](file:///workspace/js/phone-ui.js) `deleteLastTurn` 5 字段 | 直写 | 见 P1-PU2 |
| P2-B3 | [`phone-ui.js:3193, 3202`](file:///workspace/js/phone-ui.js) `renderBag` fallback | 直写 `gameState.currentBag` | 抛错而非 silent fallback |
| P2-B4 | [`phone-ui.js:577-579`](file:///workspace/js/phone-ui.js) `deleteMail` | 直写 `gameState._mails` 死字段 | 删 `_mails` 路径 |
| P2-B5 | [`game.js:1937-1940`](file:///workspace/js/game.js) `_lastChoices` 直写 | UI 状态可保留，但应明确语义 | 加注释归类为 UI 临时缓存 |

### 4.7 旧 bug（4 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P2-BUG1 | [`game.js:2583-2588, 2685-2691`](file:///workspace/js/game.js) `continue` 嵌套作用域 | `continue` 不会跳过外层 push | 用 `startsWith` + 提前构建匹配集合 |
| P2-BUG2 | [`phone-ui.js:6806-6898`](file:///workspace/js/phone-ui.js) `renderEmojiPanel` | 未清理 emoji 旧 DOM 节点 | 检查 `panel.classList.toggle` |
| P2-BUG3 | [`phone-ui.js:7343-7374`](file:///workspace/js/phone-ui.js) `openNpcDetail` cloneNode | 连续点同一 NPC 详情旧 listener 残留 | 用 `addEventListener` + `dataset.slotId` |
| P2-BUG4 | [`game.js:3845`](file:///workspace/js/game.js) `requestNpcReply` | 让主剧情页也进入 loading 状态 | 独立的 loading 状态 |

---

## 第 5 章：未修 P3（22 项）

### 5.1 死代码（5 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P3-D1 | [`phone-ui.js:1852`](file:///workspace/js/phone-ui.js) `renderForumPage` 双类型数组 | `getModulesByType(['comments', 'forum'])` 仅此一处 | 拆渲染逻辑 |
| P3-D2 | [`game.js:1728-1759`](file:///workspace/js/game.js) `cotRegex` 重复调用 | 注释里 `// === COT（思维链）处理 ===` 后存在未被消费的 `cotRegex` | 合并 replace |
| P3-D3 | [`phone-ui.js:5060-5062`](file:///workspace/js/phone-ui.js) 重复注释 | 连续两行同一注释 | 删 |
| P3-D4 | [`phone-ui.js:5089`](file:///workspace/js/phone-ui.js) `isConnected` 未用 | 计算后从未使用 | 删 |
| P3-D5 | [`phone-ui.js:7282-7290`](file:///workspace/js/phone-ui.js) `c.title` 双键 | 重复展示 | 去重 |

### 5.2 重复代码（3 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P3-R1 | [`phone-ui.js:5224-5263, 5599-5611`](file:///workspace/js/phone-ui.js) `showApiDetail` 4 处内联模板 | 完全相同 | 抽 `renderLogItemHtml(log)` |
| P3-R2 | [`phone-ui.js:201, 205, 492`](file:///workspace/js/phone-ui.js) `escapeHtml` vs `escapeAttr` 混用 | 规则分散 | 抽 `safeAttr` 包装 |
| P3-R3 | [`systems.js:852`](file:///workspace/js/systems.js) `safeInt` 局部 helper | utils.js 已有 | 统一到 utils.js |

### 5.3 性能（3 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P3-P1 | [`phone-ui.js:5224, 5241, 5253, 5299, 5602, 5624`](file:///workspace/js/phone-ui.js) `new Date(log.time).toLocaleTimeString()` | 每次都新建 Date | 复用 formatter |
| P3-P2 | [`phone-ui.js:2259-2261`](file:///workspace/js/phone-ui.js) `getDiaryName` `findIndex` 全文 | O(N²) | 用 `nameToIndex` Map |
| P3-P3 | [`phone-ui.js`](file:///workspace/js/phone-ui.js) 大量 `document.getElementById` 重复调用 | 几乎每个 render 函数都做 | 在 init 阶段缓存 DOM 引用 |

### 5.4 可访问性（2 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P3-A1 | [`phone-ui.js:7225`](file:///workspace/js/phone-ui.js) NPC 状态气泡 | `onclick="event.stopPropagation();this.classList.toggle('expanded')"` | 加 `role="button" tabindex="0"` |
| P3-A2 | [`phone-ui.js`](file:///workspace/js/phone-ui.js) `index.html` 多处 `id` 大小写不一致 | JS 写错大小写 runtime 失败 | 引入 TypeScript 风格声明或约定 kebab-case |

### 5.5 跨文件一致性问题（4 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P3-X1 | [`phone-ui.js:5606, 5609, 5627, 5629`](file:///workspace/js/phone-ui.js) `escapeHtml` 不一致 | `showApiDetail` 主函数（5224-5263）已用 escapeHtml，refresh handler 未用 | 统一 |
| P3-X2 | 多处 `Object.values(gameState.allCharacters \|\| {})` | 顺序不稳定 | 用 `Object.entries` |
| P3-X3 | [`modules.js:1613-1617`](file:///workspace/js/modules.js) / `game.js:154` `_useSysprompt` | 命名空间混乱 | 所有内部状态加下划线前缀 + 集中注册 |
| P3-X4 | 多处 `escapeHtml` | 实际只有 core.js 一份，但 game.js / systems.js / worldinfo.js 都被引为全局 | 删其它文件的局部定义 |

### 5.6 微优化与注释（5 项）

| 编号 | 位置 | 描述 | 修复 |
|------|------|------|------|
| P3-M1 | [`utils.js`](file:///workspace/js/utils.js) `safeInt("123abc")` 返回 123 | 起始数字被吞 | 写文档说明 |
| P3-M2 | [`utils.js`](file:///workspace/js/utils.js) `escapeHtml` 没处理反引号 | `` ` `` | 加 |
| P3-M3 | [`utils.js`](file:///workspace/js/utils.js) `truncateByChars` 中文按字符截断 | 可能在代理对中间切 | 检查 codePoint |
| P3-M4 | [`core.js:2669, 2673`](file:///workspace/js/core.js) 顶层 `fp` / `partial` 变量 | 每次调用重新构造正则对象 | 提到模块顶部常量 |
| P3-M5 | [`core.js:1083-1086`](file:///workspace/js/core.js) `shortReasons` / `reasonSummary` 命名混淆 | `reasonSummary` 只是 `shortReasons` 的 join | 内联 |

---

## 第 6 章：实施路线图（按 ROI 排序）

### 阶段 1：紧急 P0（4 小时内必修）
| 顺序 | 编号 | 位置 | 修复内容 | 估时 |
|------|------|------|---------|------|
| 1 | P0-2.1 | core.js:3732 | 删 `HTTP_STATUS_MAP` 死常量 + 统一 `_ERROR_MAPS` | 30 min |
| 2 | P0-2.2 | systems.js:648 | `ach.desc` 加 `escapeHtml` | 5 min |
| 3 | P0-2.3 | core.js:4451 | `setWaiting` 改走 `RuntimeState` | 30 min |
| 4 | P0-2.5 | core.js:411 | `gameState` IIFE 化（与 1.5 同步） | 2h |
| 5 | P0-2.10 | worldinfo.js:1393 + game.js:174 | 统一缓存键到 `gameState._stats.totalTurns` | 30 min |

### 阶段 2：高频 P1（1 天内）
| 顺序 | 编号 | 位置 | 修复内容 | 估时 |
|------|------|------|---------|------|
| 6 | P1-PU1 | phone-ui.js:48 | 删 `subtractPlayerMoney` + 走 `CurrencyMutator.spend` | 1h |
| 7 | P1-PU14 | game.js:2547 + 2656 | 抽 `_prepareCompressionData` / `_applyCompressionResult` | 1.5h |
| 8 | P1-PU5 | phone-ui.js:5224 | 抽 `renderLogItemHtml(log)` + 统一 escapeHtml | 1h |
| 9 | P1-PU15 | phone-ui.js 多处 | 替换内联 `onclick` → addEventListener + role/tabindex | 4h |
| 10 | P1-PU16 | UI.showModal | 加 focus-trap + ESC 关闭 | 2h |
| 11 | P1-CO1 | core.js:97 | 抽 `syncTableToState` 通用函数 | 1.5h |
| 12 | P1-CO3 | core.js:313 | 删 IIFE 轮询 + 改 TimerManager | 30 min |
| 13 | P1-CO4 | core.js:930 | 删网络检测死代码 | 30 min |
| 14 | P1-PT1 | patch.js:50 | 加 `if (!window.gameAdapter) return` 早退 | 5 min |

### 阶段 3：架构性 P1（1 周内）
| 顺序 | 编号 | 位置 | 修复内容 | 估时 |
|------|------|------|---------|------|
| 15 | P0-2.4 | core.js:5250 | `extractSetupToMemory` 走 Mutator | 2h |
| 16 | P0-2.6 | 多处 | 货币统一 `CurrencyMutator` | 1.5h |
| 17 | P0-2.7/2.8/2.9 | 多处 | 3 套存储合并为 1 套 | 3h |
| 18 | P1-MO1 | modules.js | 拆 5~8 个文件 | 1 天 |
| 19 | P1-AI1+2 | ai-contract/ | 合并 prompt-builder / response-parser / sanitizer | 4h |
| 20 | P1-SY1 + P1-CS1 | systems.js + phone-ui.css | 硬编码颜色 → CSS 变量 | 1 天 |

### 阶段 4：清理 P2 + P3（2 周内）
| 顺序 | 编号 | 描述 | 估时 |
|------|------|------|------|
| 21 | P2-D1~13 | 删除 13 项死代码 | 2h |
| 22 | P2-R1~8 | 8 项重复代码抽取 | 1 天 |
| 23 | P2-PE1~6 | 6 项性能优化 | 1 天 |
| 24 | P2-A1~5 | 5 项 a11y 补全 | 1 天 |
| 25 | P3 全 22 项 | 微优化与一致性 | 1 天 |

---

## 第 7 章：核心数据点（再次确认）

| 文件 | 行数 | 06-28 待修 | 优先级分布 |
|------|------|----------|----------|
| `js/core.js` | 5500 | 21 | P0×4 + P1×11 + P2×5 + P3×1 |
| `js/tavern-compat.js` | 5900 | 8 | P1×5 + P2×2 + P3×1 |
| `js/phone-ui.js` | 7500 | 30 | P0×3 + P1×16 + P2×8 + P3×3 |
| `js/game.js` | 4300 | 11 | P0×2 + P1×5 + P2×3 + P3×1 |
| `js/modules.js` | 4871 | 7 | P1×3 + P2×2 + P3×2 |
| `js/worldinfo.js` | 2084 | 5 | P0×1 + P1×3 + P2×1 |
| `js/systems.js` | 972 | 5 | P0×1 + P1×2 + P2×2 |
| `js/utils.js` | 551 | 4 | P2×1 + P3×3 |
| `js/init.js` | 224 | 2 | P2×1 + P3×1 |
| `js/patch.js` | 306 | 3 | P1×1 + P2×2 |
| `js/ai-contract/*` | ~600 | 4 | P1×2 + P2×2 |
| `js/state/*` | ~400 | 1 | P2×1 |
| `index.html` | 2859 | 7 | P1×2 + P2×5 |
| CSS 5 个文件 | 14023 | 8 | P0×1 + P1×1 + P2×4 + P3×2 |
| **合计** | **~51K** | **123** | **P0×16 + P1×47 + P2×38 + P3×22** |

---

## 第 8 章：维护成本估算

按"行修复后减少的长期维护成本"估算：

- **P0 修复预计减少 Bug 报告量**：约 60%（货币/标题/计数/关系四套存储引发的不一致 Bug）
- **P1 修复预计减少 a11y / 性能 / 代码理解成本**：约 30%
- **P2 修复预计减少新功能添加成本**：约 20%
- **P3 修复预计减少代码搜索时间**：约 10%

**若全部修复**：估计整体维护成本下降 **50%**，但需要投入 **约 80-100 工时**。

按用户要求"不计时间"原则，建议分 5 个 PR 阶段（每阶段对应上述 5 个阶段）完成。

---

## 附录 A：06-27 审查中已确认修复的 5 项详情

| 编号 | 修复前位置 | 修复后位置 | 修复内容 |
|------|---------|---------|---------|
| P0-1 | `tavern-compat.js:3550` | `tavern-compat.js:3550` | `addPermanentFact` 用 `upsertPermanentFact` 公共方法，split 改 `:\|：\|,\|，` |
| P0-2 | `tavern-compat.js:3546` | `tavern-compat.js:3546` | 抽 `upsertPermanentFact` 函数 |
| P0-3 | `modules.js:3270-3290` | `modules.js:3270-3290` | 自动重启不重置进度 |
| P0-4 | `tavern-compat.js:4260` | `tavern-compat.js:4260` | `saveCharacter` 深拷贝 |
| P0-5 | `css/base.css:767` | `css/base.css:767` | loading 显隐用 `.is-hidden` |

## 附录 B：建议立刻动手的"5 分钟修复"

> 这些问题改起来 5 分钟内，立竿见影，可以快速降低存量 Bug：

1. **`ach.desc` XSS** ([systems.js:648](file:///workspace/js/systems.js#L648))：5 分钟
   ```js
   // 改前
   ach.desc + '</div>'
   // 改后
   escapeHtml(ach.desc) + '</div>'
   ```

2. **`rebindBtn` 死代码** ([core.js:3948](file:///workspace/js/core.js#L3948))：30 秒删除

3. **`toggleSettingGroup` 死代码** ([core.js:4098](file:///workspace/js/core.js#L4098))：30 秒删除

4. **`isScrollNearBottom` 死代码** ([core.js:2349](file:///workspace/js/core.js#L2349))：30 秒删除

5. **`HTTP_STATUS_MAP` 死常量** ([core.js:3732](file:///workspace/js/core.js#L3732))：1 分钟删除

6. **重复注释** ([phone-ui.js:5060-5062](file:///workspace/js/phone-ui.js#L5060-L5062))：1 分钟删除一行

7. **`isConnected` 未用变量** ([phone-ui.js:5089](file:///workspace/js/phone-ui.js#L5089))：30 秒删除

8. **`gameAdapter` 不存在时 patch.js 早退** ([patch.js:50](file:///workspace/js/patch.js#L50))：2 分钟加 `if (!window.gameAdapter) return;`

9. **iOS 17+ 防双击 touchend 删除** ([patch.js:240-246](file:///workspace/js/patch.js#L240-L246))：2 分钟删

10. **showApiDetail refresh handler XSS 修复** ([phone-ui.js:5605-5609](file:///workspace/js/phone-ui.js#L5605-L5609))：2 分钟加 `escapeHtml`

**合计：约 15 分钟可消除 5 个 P0/P1 + 5 个 P3 问题，立省未来 10+ 工时的维护成本。**

---

> 报告完毕。本次审查**未对任何文件做修改**，所有问题均只做研究和列举。
> 后续若要按本报告修复，建议按第 6 章 5 阶段顺序进行，每个 PR 一个阶段。
