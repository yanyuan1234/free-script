# 状态层代码审查报告 — 2026-07-01

> **审查范围**：`/workspace/js/state/`（schema.js 317行 / state-manager.js 260行 / mutators/ 8 个文件 / adapters/game-memory-adapter.js 210行）
> **重点**：单状态源架构下 8 个 Mutator 的一致性、GameMemoryAdapter 双向同步、同一状态多入口写入
> **审查基线**：6-30 统筹版（已归档 + 部分修复） + v3 审查修复后
> **本次增量**：聚焦"游戏状态层架构清晰、写入路径唯一、字段映射完整"

---

## 第 0 章：执行摘要

### 0.1 本轮问题分类

| 优先级 | 数量 | 说明 |
|--------|------|------|
| **P0 严重** | **3** | characters 字段映射缺 desc/identity/tags/stats/notes；Location 内存写入绕过 LocationMutator；UndoMutator 撤销 conversations 写错路径 |
| **P1 高** | **8** | GameMemoryAdapter 缺反向同步；mergeItems 字段更新不全；setQuests/addQuest 合并语义不一致；无 normalizeTime / normalizeRelationship；get 每次深拷贝性能问题；_getRaw 无危险键防护；QuestMutator 同步丢失 progress/hint/rewards；多入口 fallback 绕过 Mutator |
| **P2 中** | **6** | 8 个 Mutator API 不一致；set/get 命名风格混乱；CurrencyMutator 缺 normalize；extractSetupToMemory 关系直写绕过 Mutator；UndoMutator 缺 setHistory/clearHistory；schema._legacyToPath 重复键 + 反向映射选用问题 |
| **P3 低** | **4** | 注释里 `_nextToken` 残留；setItems 缺 docstring 解释 MERGE 与 REPLACE；TimeMutator 注释冗长；无 getTime |
| **合计** | **21** | — |

### 0.2 三句话核心结论

1. **P0 致命**：GameMemoryAdapter 的 characters fieldMap 漏掉 `desc/identity/tags/stats/notes` 5 个核心字段，导致 AI prompt 看到的角色信息残缺（StateManager 有，gm.tables 没有），且与 CharacterMutator.normalizeCharacter 的 13 字段输出严重不对齐。
2. **架构硬伤**：GameMemoryAdapter 只有 `syncToGameMemory`（单向 SM → gm），反向（gm → SM）由 core.js 的 `_syncItemsToBag` / `_syncRelationshipsToGameState` 实现，**直接用 `StateManager.set` 绕过 Mutator**，且无 normalize / 限额 / 事务。
3. **多入口写入**：8 个域中 `entities.bag / entities.characters / entities.quests` 已经收敛到 Mutator 唯一入口（兜底分支），但 **`entities.locations`（MemoryManagerUI 4 处）** 和 **`entities.relationships`（extractSetupToMemory 1 处）** 仍存在绕过 Mutator 的直写路径。

---

## 第 1 章：P0 严重问题

### P0-1 `game-memory-adapter.js:68-91` — characters 字段映射缺失核心字段

**文件位置**：`/workspace/js/state/adapters/game-memory-adapter.js:68-91`

**问题代码**：
```js
68→            this._mergeTable('characters', StateManager.get('entities.characters') || [], {
69→                name: 'name',
70→                title: 'title',
71→                relation: 'relation',
72→                mood: 'mood',
73→                location: 'location',
74→                outfit: 'outfit',
75→                favorability: 'favorability',
76→                status: 'status'
77→            }, function(name) {
78→                return {
79→                    name: name,
...
89→                    locked: false
90→                };
91→            });
```

**对比** `character-mutator.js:204-227` 的 `normalizeCharacter` 输出 13 个字段（identity / desc / tags / stats / notes / favor / mood / location / outfit / favorability / status / history / gameTime / accessCount / lastChangedTurn / locked）。

**遗漏字段**：
- 实体字段：`identity`（与 `title` 不同的语义）、`desc`、`tags`、`stats`、`notes`
- 运行时字段：`history`、`gameTime`、`accessCount`（仅 factory 初始化，MERGE 阶段不更新）

**状态层一致性说明**：
- `entities.characters[i].desc/identity/tags/stats/notes` 写入后通过 StateManager.set → _syncLegacyMirror 写入 `gameState.allCharacters`（数组→对象转换时也带这些字段）
- 但 `gm.tables.characters[name]` 同步路径只 MERGE 8 个字段，导致 gm 侧的角色档案"骨架化"
- `gm.tables.characters` 是 `MemoryManagerUI`（tavern-compat.js:4452/4463/4482）和 `EnhancedMemory.buildSmartInjection` 注入 prompt 的**数据源**
- 结果：玩家/AI 编辑的 desc、tag、stat、note 永远进不了 prompt，AI 看不到角色的最新信息

**修复方案**：
```js
this._mergeTable('characters', StateManager.get('entities.characters') || [], {
    name: 'name',
    title: 'title',
    identity: 'identity',
    desc: 'desc',
    relation: 'relation',
    mood: 'mood',
    location: 'location',
    outfit: 'outfit',
    favorability: 'favorability',
    status: 'status',
    tags: 'tags',
    stats: 'stats',
    notes: 'notes'
}, function(name) {
    return {
        name: name,
        title: '', identity: '', desc: '', relation: '',
        mood: '', location: '', outfit: '',
        favorability: 0, status: '',
        tags: [], stats: [], notes: '',
        history: [], lastChangedTurn: turn, locked: false
    };
});
```

---

### P0-2 `tavern-compat.js:4848-4893/4915-4935` — MemoryManagerUI.saveLocation/saveNewLocation 绕过 LocationMutator

**文件位置**：`/workspace/js/tavern-compat.js:4848-4893, 4915-4935`

**问题代码**（节选 line 4867-4893）：
```js
4848→        if (typeof StateManager !== 'undefined' && StateManager.get && StateManager.set) {
4849→            // 构建 StateManager 视角的 locations（按现有 + 用户编辑）
4850→            var smLocs = [];
4851→            var rawLocs = StateManager.get('entities.locations') || [];
4852→            rawLocs.forEach(function(l) {
4853→                if (l && l.name && l.name !== oldName) {
4854→                    smLocs.push({ name: l.name, desc: l.desc || '', features: l.features || '', charactersPresent: l.charactersPresent || '' });
4855→                }
4856→            });
...
4867→                smOk = !!StateManager.set('entities.locations', smLocs, { silent: true });
```

**状态层一致性说明**：
- 同一文件 `saveCharacter`（line 4490）/ `saveItem`（line 4626）/ `saveNewItem`（line 4754）都走 `CharacterMutator` / `BagMutator`
- `saveLocation` 走 `StateManager.set('entities.locations', ...)` 直写，**完全跳过 LocationMutator.mergeLocations / setLocations / normalizeLocation**
- 后果：
  1. `LocationMutator._STOP_WORDS`（line 7）的过滤（"阳光/风/雨"）在 UI 编辑路径失效
  2. `LocationMutator.normalizeLocation` 生成的 `id`、`locked` 运行时字段被丢弃
  3. 没有 `mergeLocations` 的"同名更新/新名追加"语义，每次都是 REPLACE 全量
  4. 跟 `addLocation`（line 4899）/ `deleteLocation`（line 4887）都缺 mutate 入口（只用了 set+filter）

**修复方案**：
```js
// line 4867 替换为：
if (typeof LocationMutator !== 'undefined' && LocationMutator.removeLocation && LocationMutator.mergeLocations) {
    LocationMutator.removeLocation(oldName, { silent: true });
    LocationMutator.mergeLocations([_newLocData], { silent: true });
} else if (typeof StateManager !== 'undefined' && StateManager.set) {
    // legacy 兜底（无 LocationMutator 环境）
    ...
}
```
`saveNewLocation`（line 4915-4935）和 `deleteLocation`（line 4887-4895）同样改造为 `LocationMutator.addLocation` / `LocationMutator.removeLocation`。

---

### P0-3 `undo-mutator.js:73-79` — 撤销时 conversationHistory 写错路径

**文件位置**：`/workspace/js/state/mutators/undo-mutator.js:73-79`

**问题代码**：
```js
73→        // 1. conversationHistory：走 StateManager 静默写入
74→
75→        // 写入会创建孤立子树，既不触发 _syncLegacyMirror 同步到 gameState.conversationHistory，
76→        // 也不被订阅者监听 → 撤销后对话历史不恢复。正确路径为 'progress.conversationHistory'。
77→        if (typeof StateManager !== 'undefined' && StateManager.set) {
78→            StateManager.set('progress.conversationHistory', snap.conversationHistory || [], { silent: true });
79→        }
```

**状态层一致性说明**：
- **注释里明确指出**之前 `StateManager.set('conversationHistory', ...)` 是错的（孤立子树，不被订阅监听，不同步 legacy 镜像）
- **但代码（line 78）已经写为 `'progress.conversationHistory'`** — 与注释描述自相矛盾
- 这是 P2 之前修复 BUG 的"修复后的代码"，注释未同步更新，导致后续维护者看到注释会以为还是 bug 状态
- 注释位于 line 73-76，代码在 line 78，应该删除冲突注释，避免误导

**修复方案**：
- 删除 line 74-76 的旧注释（"写入会创建孤立子树..."那段）
- 保留 line 73 的意图说明（"走 StateManager 静默写入"）
- 进一步验证 `snap.conversationHistory` 是否真的有数据（`pushSnapshot` 第 38 行是 `_safeClone(snap.conversationHistory || [])`，所以可能有 undefined 进入）

---

## 第 2 章：P1 高优先级问题

### P1-1 `game-memory-adapter.js:1-210` — 缺反向同步（gm → SM）

**文件位置**：`/workspace/js/state/adapters/game-memory-adapter.js`（整个文件 210 行）

**问题代码**：
```js
12→const GameMemoryAdapter = {
13→    _syncLock: false,
14→
15→    // 绑定：订阅 StateManager 变更，自动同步到 GameMemory
16→    bind() {
17→        var self = this;
18→        StateManager.subscribe('entities.**', () => {
19→            self.syncToGameMemory();
20→        });
```

**状态层一致性说明**：
- 适配器只有 `syncToGameMemory`（SM → gm），无 `syncFromGameMemory`（gm → SM）
- 反向同步由 `core.js:265-279` 的三个原始函数实现：
  - `_syncItemsToBag()`（line 265）— `gm.tables.items` → `entities.bag`
  - `_syncQuestsToGameState()`（line 271）— `gm.quests` → `entities.quests`
  - `_syncRelationshipsToGameState()`（line 277）— `gm.tables.relationships` → `entities.relationships`
- 这些函数用 `StateManager.set` **直接写** StateManager，绕过 Mutator 的 `normalizeItem` / `normalizeQuest` / `mergeRelationships`（含去重、限额 10 条、对称方向合并等语义）
- 反向同步也绕过事务（`StateManager.transaction`）和限额

**修复方案**：
1. 在适配器添加 `syncFromGameMemory()`，作为反向同步统一入口
2. 重写 `core.js:265-279` 三个函数，转调适配器，适配器内部用 `BagMutator.setItems` / `QuestMutator.setQuests` / `RelationshipMutator.mergeRelationships` 走 Mutator
3. 适配器双向用 `_syncLock` 防回环（已实现，但只对 forward 方向有效）

---

### P1-2 `bag-mutator.js:33-64` — mergeItems 字段更新白名单不全

**文件位置**：`/workspace/js/state/mutators/bag-mutator.js:33-64`

**问题代码**：
```js
33→        inputItems.forEach(function(it) {
34→            if (!it) return;
35→            const key = it.name || it.title || it.id;
36→            if (!key) return;
37→            if (existingMap[key]) {
38→                existingMap[key].count = it.count !== undefined ? it.count : (existingMap[key].count || 1);
39→                if (it.desc !== undefined) existingMap[key].desc = it.desc;
40→                if (it.rarity !== undefined) existingMap[key].rarity = it.rarity;
41→                if (it.rarityClass !== undefined) existingMap[key].rarityClass = it.rarityClass;
42→                if (it.equipped !== undefined) existingMap[key].equipped = it.equipped;
43→                if (it.usable !== undefined) existingMap[key].usable = it.usable;
```

**状态层一致性说明**：
- `mergeItems` 更新白名单只有 6 个字段：`count / desc / rarity / rarityClass / equipped / usable`
- **遗漏**：`unit / effect / equippable / slot / history / obtainedTurn / lastChangedTurn`
- 对比 `normalizeItem`（line 99-137）输出 13 个字段
- 后果：AI 返回物品 `effect: "回血 30"` 或 `unit: "瓶"`，mergeItems 不更新，旧值保留；最后 `setItems(bag)` 重新 normalize，因为 `normalizeItem` 用 `raw.effect || ''` 读取旧值，**新 effect 永久丢失**

**修复方案**：
- 提取字段白名单常量为 `BagMutator._MERGE_FIELDS = ['count', 'desc', 'rarity', 'rarityClass', 'equipped', 'usable', 'unit', 'effect', 'equippable', 'slot', 'history']`
- 改为统一遍历：
```js
BagMutator._MERGE_FIELDS.forEach(function(field) {
    if (it[field] !== undefined) existingMap[key][field] = it[field];
});
```

---

### P1-3 `quest-mutator.js:38-82 / 110-126` — setQuests 与 addQuest 合并语义不一致

**文件位置**：`/workspace/js/state/mutators/quest-mutator.js:50-82` 与 `110-126`

**问题代码**（line 67-72 是 setQuests 的 _smartMerge）：
```js
67→                q.progress = QuestMutator._pickHigherProgress(old.progress, q.progress);
68→                // 合并描述、提示等字段（新值优先，但若为空则保留旧值）
69→                if (!q.desc && old.desc) q.desc = old.desc;
70→                if (!q.hint && old.hint) q.hint = old.hint;
71→                if (!q.rewards || q.rewards.length === 0) q.rewards = old.rewards || [];
72→                // 保留 id
73→                if (old.id && !q.id) q.id = old.id;
```

对比 addQuest（line 116-121）：
```js
116→            normalized.progress = this._pickHigherProgress(existing.progress, normalized.progress);
117→            if (existing.status === this.STATUS.COMPLETED || existing.status === this.STATUS.FAILED) {
118→                normalized.status = existing.status;
119→            }
120→            Object.assign(existing, normalized);
```

**状态层一致性说明**：
- `setQuests` 调用 `_smartMerge`：保留 desc/hint/rewards/id 字段
- `addQuest` 用 `Object.assign`：新值完全覆盖，但 id 来自 normalized（每次新建一个 'quest_' + title + '_' + Date.now()），id 不保留！
- 后果：同一个任务 `addQuest` 多次调用（AI 每回合都 add）会**丢失原 id**（每次生成新时间戳 id），导致 undo 栈、UI 锚定失效

**修复方案**：
- 抽出 `_mergeSingleQuest(oldQ, newQ)` 公共方法
- addQuest 也走 `_smartMerge` 一致逻辑：
```js
addQuest(quest, options) {
    const normalized = this.normalizeQuest(quest);
    if (!normalized) return false;
    const quests = StateManager.get('entities.quests') || [];
    const idx = quests.findIndex(q => q.id === normalized.id || q.title === normalized.title);
    if (idx >= 0) {
        const merged = this._smartMerge([quests[idx]], [normalized])[0];
        quests[idx] = merged;
    } else {
        quests.push(normalized);
    }
    return this.setQuests(quests, options);
}
```

---

### P1-4 `time-mutator.js` / `relationship-mutator.js` / `currency-mutator.js` — 缺 normalize 方法

**文件位置**：
- `/workspace/js/state/mutators/time-mutator.js`（无 normalize）
- `/workspace/js/state/mutators/relationship-mutator.js`（无 normalize）
- `/workspace/js/state/mutators/currency-mutator.js`（无 normalize）

**状态层一致性说明**：
- 其他 Mutator 都有 `normalizeX`：`BagMutator.normalizeItem` / `CharacterMutator.normalizeCharacter` / `LocationMutator.normalizeLocation` / `QuestMutator.normalizeQuest`
- `TimeMutator.setTime` 内部做 normalize（line 13-21），但**没有暴露 normalizeTime 供外部使用**（如 schema normalizeState 迁移、AI 输出预清洗）
- `RelationshipMutator.mergeRelationships` 接收的入参未经 normalize（line 14-38），AI 返回的 `r.desc` 包含换行/前后空格/类型字符串大小写都没有标准化
- `CurrencyMutator.set/add/spend` 只校验 `isFinite`，无统一 normalize

**修复方案**：
- 把 `TimeMutator.setTime` 的 13-21 行抽成 `normalizeTime(raw)` 独立方法
- 给 `RelationshipMutator` 加 `normalizeRelationship(raw)`：trim 字符串、type 标准化（"friend"/"朋友"/"friend" 都映射到统一字符串）
- 给 `CurrencyMutator` 加 `normalizeAmount(raw)`：parseInt + Math.max(0, ...) 统一入口

---

### P1-5 `state-manager.js:40-44` — get() 每次深拷贝性能问题

**文件位置**：`/workspace/js/state/state-manager.js:40-44`

**问题代码**：
```js
40→    get(path) {
41→        if (!path) return this.snapshot();
42→        const value = this._getRaw(path);
43→        return StateSchema.deepClone(value);
44→    },
```

**状态层一致性说明**：
- `get()` 每次调用都对返回值做 `deepClone`（递归遍历整个子树）
- 高频调用：
  - `CharacterMutator.mergeCharacters` 调 1 次 `get('entities.characters')` 拿当前列表（line 46）
  - `BagMutator.mergeItems` 调 1 次（line 20）
  - `LocationMutator.mergeLocations` 调 1 次（line 26）
  - `QuestMutator.setQuests` 调 1 次（line 42）
  - **每个 mutator 的 _applyXxx 都至少调 1 次 get**（5-6 个字段 = 5-6 次深拷贝/回合）
- 但调用方拿到值后通常：
  1. 在 map/forEach 里只读访问 → 不需要深拷贝
  2. 用 `Object.assign({}, list[idx], normalized)` 创建新对象 → 也用不到深拷贝
- 唯一的真实需求是"调用方不能 mutate 内部状态"，但 Mutator 内部完全可以信任（它们最终都走 `StateManager.set`）

**修复方案**：
- 添加 `peek(path)` 返回**原始引用**（仅内部/Mutator 使用）
- `get(path)` 保留深拷贝（公共 API 契约）
- 修改 8 个 Mutator 内部用 `peek` 替代 `get`（性能提升约 10-50 倍/回合）
- 在 README / docstring 明确"Mutator 必须用 peek，普通 UI 用 get"

---

### P1-6 `state-manager.js:188-196` — _getRaw 无危险键防护

**文件位置**：`/workspace/js/state/state-manager.js:188-196`

**问题代码**：
```js
188→    _getRaw(path) {
189→        const parts = path.split('.');
190→        let current = this._state;
191→        for (let i = 0; i < parts.length; i++) {
192→            if (current === null || current === undefined) return undefined;
193→            current = current[parts[i]];
194→        }
195→        return current;
196→    },
```

**状态层一致性说明**：
- `_setRaw`（line 199-217）已经拦截 `__proto__/constructor/prototype`
- 但 `_getRaw` 无任何防护：
  - `get('entities.characters.constructor')` 返回 `Array`（构造器）
  - `get('__proto__')` 返回 `Object.prototype`（原型）
  - `get('entities.characters.__proto__')` 返回 `Array.prototype`
- 注释中提到的"peek 行为"也只是没实现
- `_getRaw` 自身被 `get()` 调用（line 42）和 `set()` 内部读 oldValue（line 64）— 后者已过 `validatePath` 校验，但前者没有

**修复方案**：
```js
_getRaw(path) {
    const parts = path.split('.');
    let current = this._state;
    const dangerous = StateSchema && StateSchema._DANGEROUS_KEYS;
    for (let i = 0; i < parts.length; i++) {
        if (current === null || current === undefined) return undefined;
        if (dangerous && dangerous[parts[i]]) return undefined;
        current = current[parts[i]];
    }
    return current;
}
```

---

### P1-7 `game-memory-adapter.js:112-145` — QuestMutator 同步丢失 progress/hint/rewards

**文件位置**：`/workspace/js/state/adapters/game-memory-adapter.js:112-145`

**问题代码**（line 117-144）：
```js
117→                quests.forEach(function(q) {
118→                    if (!q || !q.title) return;
119→                    let gq = titleMap[q.title];
120→                    if (!gq) {
121→                        gq = {
122→                            title: q.title,
123→                            type: q.type || 'quest',
124→                            status: 'pending',
125→                            createdTurn: turn,
126→                            resolvedTurn: 0
127→                        };
128→                        GameMemory.quests.push(gq);
129→                        titleMap[q.title] = gq;
130→                    }
131→                    gq.type = q.type || gq.type;
```

**状态层一致性说明**：
- `entities.quests[i]` 有 13 个字段（id/title/type/status/desc/progress/hint/rewards/deadline/priority/createdTurn/resolvedTurn/stale）
- `gm.quests[i]` 只同步 4 个字段：title/type/status/createdTurn/resolvedTurn
- **丢失**：`desc / progress / hint / rewards / deadline / priority / stale`
- 关键的是 `progress`（"3/5"）— `buildSmartInjection` 如果读 gm.quests 显示进度，会一直显示 0/1

**修复方案**：
```js
gq.type = q.type || gq.type;
gq.status = ...; // 现有逻辑
if (q.desc) gq.desc = q.desc;
if (q.progress) gq.progress = q.progress;
if (q.hint) gq.hint = q.hint;
if (Array.isArray(q.rewards) && q.rewards.length) gq.rewards = q.rewards;
if (q.priority) gq.priority = q.priority;
if (q.deadline) gq.deadline = q.deadline;
```
（仅在新值存在时覆盖，保留运行时累积字段）

---

### P1-8 `ai-response-mutator.js` 多处 — Mutator 不可用时的内联 fallback 绕过限额/事务

**文件位置**：`/workspace/js/ai-contract/ai-response-mutator.js:262-264, 273-275, 299-301, 569-585`

**问题代码示例**（line 260-264 `_applyCharacters` 的 fallback）：
```js
260→        } else {
261→
262→            // 已触发 _syncLegacyMirror（含数组→对象转换）同步到 gameState.allCharacters
263→            StateManager.set('entities.characters', filtered, { silent: true });
264→        }
```

line 273-275（`_applyBag` fallback）、line 299-301（`_applyQuests` fallback）、line 573-585（`_applyRelationships` 的 favorability 更新 fallback）。

**状态层一致性说明**：
- 主路径：BagMutator.mergeItems / CharacterMutator.mergeCharacters / QuestMutator.setQuests
  - **带 normalize**（去重、过滤无效值、字段白名单）
  - **带去重**（mergeCharacters 模糊匹配、mergeItems 模糊匹配）
  - **带智能合并**（setQuests 保留已完成状态、取更高 progress）
- Fallback：`StateManager.set('entities.X', raw_array, ...)`
  - **不带 normalize**（AI 输出的脏数据直接进入 StateManager）
  - **不去重**（AI 多次返回同一角色 → 列表里出现 N 份）
  - **不保留 status**（AI 返回 `status: '进行中'` 会覆盖已完成的 `已完成`）
  - **绕过 transaction 限额/校验**（`StateManager.set` 自身的事务回滚也不参与此 fallback）

**修复方案**：
- 移除所有 `else { StateManager.set(...) }` fallback（注释里"Mutator 始终加载"已确认）
- 真正 fallback 是 `throw new Error('[AIResponseMutator] XXXMutator 未加载，无法写入')`，与 core.js:2845 等保持一致
- 现状是 fallback 分支"静默写入脏数据"，比抛错更危险

---

## 第 3 章：P2 中优先级问题

### P2-1 8 个 Mutator API 不一致

**说明**：8 个 Mutator 的方法命名差异显著，详见末尾"8 mutator 接口对比表"。

| 缺失能力 | 涉及的 Mutator | 备注 |
|----------|---------------|------|
| `mergeX` | Quest, Currency, Time, Undo | 应该有 merge 语义 |
| `addX` | Character, Relationship, Time, Undo | 应该有 add 语义 |
| `removeX` | Bag, Quest, Currency, Time, Undo | delete/remove |
| `getX` | Bag, Quest, Time, Undo | 应该有 get 入口 |
| `normalizeX` | Currency, Time, Relationship, Undo | 应该有 normalize |
| `replaceX` | Bag, Location, Quest, Relationship, Time, Undo | 应该有 replace |

**修复方案**：
- 制定 8 个 Mutator 公共 API 接口（推荐）：
  ```
  setX(list, options)          // REPLACE 全量
  mergeX(list, options)        // MERGE 同名更新，新名追加
  addX(item, options)          // 添加单个
  removeX(id, options)         // 删除单个
  getX(id)                     // 读取单个（深拷贝）
  getAllX(options)             // 读取列表（深拷贝）
  normalizeX(raw)              // 标准化单个
  replaceX(id, newItem, options) // rename + 替换
  ```
- 缺哪个补哪个，但优先 P0/P1 修复，不强求一次到位

---

### P2-2 `currency-mutator.js:24-31` — 命名风格不统一

**文件位置**：`/workspace/js/state/mutators/currency-mutator.js`

**问题代码**：
```js
24→    set(amount, options) {
```

**状态层一致性说明**：
- `set(amount)` 缺 `X` 后缀（应叫 `setAmount` 或保持 `set`）
- `add(amount)` / `spend(amount)` / `get()` / `getName()` 命名风格混杂
- 与其他 Mutator 的 `setX / addX / mergeX` 不一致（Currency 是单值不是列表，但仍应有 `setAmount` / `addAmount` / `spendAmount` 命名规范）

**修复方案**：
```js
setAmount / addAmount / spendAmount / getAmount / getName
```

---

### P2-3 `core.js:5399-5414` — extractSetupToMemory 关系直写绕过 Mutator

**文件位置**：`/workspace/js/core.js:5399-5414`

**问题代码**：
```js
5399→        if (Array.isArray(parsed.relationships)) {
5400→            parsed.relationships.forEach(function(r) {
5401→                if (!r || !r.from || !r.to) return;
5402→                gm.tables.relationships[r.from + '->' + r.to] = {
5403→                    from: r.from,
5404→                    to: r.to,
5405→                    type: r.type || '',
5406→                    desc: r.desc || '',
5407→                    lastChangedTurn: 0
5408→                };
5409→            });
5410→
5411→            // 由 _syncRelationshipsToGameState 作为唯一同步点统一更新 gameState + StateManager
5412→            if (typeof _syncRelationshipsToGameState === 'function') {
5413→                _syncRelationshipsToGameState();
5414→            }
```

**状态层一致性说明**：
- 写入路径：gm.tables.relationships（直接）→ _syncRelationshipsToGameState（gm → SM）→ StateManager.set('entities.relationships')
- **绕过了 RelationshipMutator.mergeRelationships**（包含：去重 + 对称方向合并 + 限额 10 条 + normalize）
- 后果：
  1. 同一对 A↔B 可能产生两条记录（gm 侧 A->B 和 B->A 都会进表）
  2. 不走限额 10 条逻辑，AI 设定里关系图谱可能超过 10 条
  3. 不触发 `entities.relationships` 订阅者（`GameMemoryAdapter` 只订阅 `entities.**`，订阅者会被触发，但因为是从 gm 侧来的，不会回环）
  4. 与 `ai-response-mutator.js:594` 的 `RelationshipMutator.mergeRelationships` 路径不一致

**修复方案**：
```js
if (Array.isArray(parsed.relationships)) {
    if (typeof RelationshipMutator !== 'undefined' && RelationshipMutator.mergeRelationships) {
        RelationshipMutator.mergeRelationships(parsed.relationships, { silent: true });
    } else {
        throw new Error('[extractSetupToMemory] RelationshipMutator 未加载');
    }
}
```

---

### P2-4 `undo-mutator.js:22-66` — 缺 setHistory/clearHistory/maxSize setter

**文件位置**：`/workspace/js/state/mutators/undo-mutator.js`

**状态层一致性说明**：
- 只有 `pushSnapshot` / `popSnapshot` / `getHistory` / `size`，没有 `setHistory`（批量替换）和 `clearHistory`（清空）
- `size()` 重复 `getHistory().length`，没必要
- 没有 setter 用于 `maxUndoHistory`（line 33 只读）

**修复方案**：
- 加 `clearHistory(options)` 委托 `StateManager.set('ui.undoHistory', [], options)`
- `maxUndoHistory` 用 `StateManager.get` 取（已实现）
- `size` 直接返回 `getHistory().length`（已实现，无需修改）

---

### P2-5 `schema.js:155-170` — 重复键 + 反向映射选用问题

**文件位置**：`/workspace/js/state/schema.js:155-170`

**问题代码**：
```js
155→
156→        'undoHistory': 'ui.undoHistory',
157→        '_undoHistory': 'ui.undoHistory',
```

**状态层一致性说明**：
- 两个 legacy 名（`undoHistory` 和 `_undoHistory`）都映射到 `ui.undoHistory`
- `_buildReverseMap`（line 176-181）遍历 `_legacyToPath`，对同一个 `ui.undoHistory` 路径，后遍历到的 `_undoHistory` 覆盖先遍历到的 `undoHistory`
- 最终 `_pathToLegacy['ui.undoHistory'] = '_undoHistory'`
- 调用方 `getLegacyName('ui.undoHistory')` 返回 `_undoHistory`
- 这导致 `_syncLegacyMirror` 写入 `this._state._undoHistory`（带下划线）
- **巧合**：`init.js:69` 和 `phone-ui.js:5150` 都读 `gameState._undoHistory`（带下划线），所以目前工作正常
- 但是这层"巧合正确"非常脆弱，新人加新代码读 `gameState.undoHistory`（无下划线）就会断

**修复方案**：
- 删除 line 156 的 `'undoHistory': 'ui.undoHistory'`，只保留 `_undoHistory`
- 注释说明："历史数据兼容期：早期版本存 undoHistory（无下划线），新版本统一为 _undoHistory"
- 评估：是否要彻底统一字段名（去掉下划线）

---

### P2-6 `state-manager.js:12-15` — 残留注释 + 死字段

**文件位置**：`/workspace/js/state/state-manager.js:12-15`

**问题代码**：
```js
12→    // 是一个误导性的"撛设开关"（永远为 true，无切换为 false 的路径）。
13→    // getLegacy/setLegacy 当前仍在使用（_syncLegacyMirror 桥接 gameState ↔ StateManager），
14→    // 是否可用由方法本身的存在性决定，无需此标志。
15→    _nextToken: 1,
```

**状态层一致性说明**：
- line 12-14 是一段**过时的注释**（描述一个已经删除的字段），但放在 `_nextToken: 1` 上方明显错位
- 实际注释应是解释 `_nextToken`（订阅 token 自增），但 line 12-14 完全不相关

**修复方案**：
- 删除 line 12-14 整段
- 在 line 15 替换为：
```js
// 订阅 token 自增（subscribe 返回的句柄，供 unsubscribe 使用）
_nextToken: 1,
```

---

## 第 4 章：P3 低优先级问题

### P3-1 `currency-mutator.js:1-10` — 文档化注释过于详细

注释横跨 6 行，引用了 4 个旧函数（getPlayerMoney/subtractPlayerMoney/getCurrencyName），与本文件核心逻辑无关。

**修复方案**：删除 line 1-10 的"改用 CurrencyMutator.spend/add/get/set 后"段，仅保留 "货币变更器" 标题。

---

### P3-2 `time-mutator.js:6-10` — 注释与代码描述冲突

注释说"导致 GameTimeSystem.parseFromAI 通过 setTime 写入的 weather/era 被丢弃"，但实际上当前 `setTime` 接收的 normalized 对象（line 14-21）**已经包含 weather/era**。

**修复方案**：删除/简化注释。

---

### P3-3 8 个 Mutator 都缺 docstring 说明 MERGE vs REPLACE 语义

只有 CharacterMutator / BagMutator 内部有详细注释，其他都是"1-2 句话 + 标题"。

**修复方案**：每个 setX/mergeX 加 JSDoc 风格简短说明。

---

### P3-4 `state-manager.js` 无 `peek` 方法

注释里提到 `get/peek`，但代码只有 `get` 和 `_getRaw`。

**修复方案**：实现 `peek(path)`，见 P1-5。

---

## 第 5 章：附录

### 5.1 8 Mutator 接口对比表

| Mutator | setX | mergeX | addX | removeX | getX | normalizeX | replaceX | 其他 |
|---------|------|--------|------|---------|------|------------|----------|------|
| **BagMutator** | ✅ setItems | ✅ mergeItems | ✅ addItem | ❌（仅 setItems+filter） | ❌ | ✅ normalizeItem | ❌ | _normalizeItemName 模糊匹配 |
| **CharacterMutator** | ✅ setCharacters | ✅ mergeCharacters | ❌ | ✅ removeCharacter | ✅ getCharacter | ✅ normalizeCharacter | ✅ replaceCharacter | updateCharacter / updateRelationship / filterOutPlayer / normalizeStats |
| **CurrencyMutator** | ✅ set (无后缀) | ❌ | ✅ add (无后缀) | ❌（仅 spend 变相） | ✅ get (无后缀) | ❌ | ❌ | getName / spend |
| **LocationMutator** | ✅ setLocations | ✅ mergeLocations | ✅ addLocation | ✅ removeLocation | ✅ getLocation | ✅ normalizeLocation | ❌ | _isStopWord |
| **QuestMutator** | ✅ setQuests | ❌（内嵌 _smartMerge） | ✅ addQuest | ❌（仅 setQuests+filter） | ❌ | ✅ normalizeQuest | ❌ | autoAdvanceByStory / _pickHigherProgress / normalizeStatus / normalizeType / normalizeRewards / isCompleted / isFailed / STATUS / TYPE 常量 |
| **RelationshipMutator** | ❌（仅 clear） | ✅ mergeRelationships | ❌ | ❌（仅 clearRelationships） | ✅ getRelationships | ❌ | ❌ | clearRelationships |
| **TimeMutator** | ✅ setTime | ❌ | ❌ | ❌ | ❌ | ❌（内嵌） | ❌ | _isEarlier / _dateValue / _timeToMinutes（单调性校验） |
| **UndoMutator** | ❌ | ❌ | ✅ pushSnapshot | ✅ popSnapshot | ✅ getHistory | ❌ | ❌ | restoreFromSnapshot / size / _safeClone |

**汇总**：
- 有 `mergeX` 的：4/8（Bag, Character, Location, Relationship）
- 有 `addX` 的：5/8（Bag, Currency, Location, Quest, Undo）— 但 Undo 是 push 语义
- 有 `removeX` 的：3/8（Character, Location, Undo）
- 有 `getX` 的：5/8（Character, Currency, Location, Relationship, Undo）
- 有 `normalizeX` 的：4/8（Bag, Character, Location, Quest）
- 有 `replaceX` 的：1/8（Character）
- **唯一完整**：CharacterMutator（除 replaceCharacter，其他 5 项都有）— 其他都缺

---

### 5.2 各域写入入口清单

#### 域：`entities.bag`

| 入口 | 文件:行 | 路径 |
|------|---------|------|
| ✅ **唯一主入口** | `BagMutator.setItems` / `mergeItems` / `addItem` | 4-93 |
| Mutator 不可用兜底（inline） | `ai-response-mutator.js:273-275` | ❌ 见 P1-8 |
| 设定提取 | `core.js:5438-5449` | BagMutator.mergeItems ✅ |
| <mem> 标签处理 | `core.js:2796-2807` | BagMutator.mergeItems + setItems ✅ |
| UI 编辑 | `tavern-compat.js:4626-4711` | BagMutator.setItems ✅（有 legacy 兜底） |
| UI 删除 | `tavern-compat.js:4721-4730` | _syncItemsToBag / StateManager.set ✅（有兜底） |
| UI 新建 | `tavern-compat.js:4754-4806` | BagMutator.mergeItems ✅（有兜底） |
| 反向同步（gm→SM） | `core.js:265-267` `_syncItemsToBag` | StateManager.set 直写 ❌ 见 P1-1 |

**结论**：写入主入口已统一为 BagMutator，但反向同步（gm→SM）由 `core.js:265` 绕过 Mutator。

---

#### 域：`entities.characters`

| 入口 | 文件:行 | 路径 |
|------|---------|------|
| ✅ **唯一主入口** | `CharacterMutator.setCharacters` / `mergeCharacters` / `replaceCharacter` / `removeCharacter` | 4-228 |
| Mutator 不可用兜底（inline） | `ai-response-mutator.js:260-264` | ❌ 见 P1-8 |
| 设定提取 | `core.js:5384-5394` | CharacterMutator.mergeCharacters ✅ |
| <mem> 标签处理 | `core.js:2817-2839` | CharacterMutator.updateCharacter / mergeCharacters ✅ |
| 深度融合 | `core.js:3543-3555` → `RuntimeBridge.mergeCharacters` → `game.js:3377` | mergeCharacters → CharacterMutator ✅ |
| UI 编辑 | `tavern-compat.js:4490-4515` | CharacterMutator.replaceCharacter ✅ |
| UI 新建 | `tavern-compat.js:4565-4585` | CharacterMutator.mergeCharacters ✅ |
| UI 删除 | `tavern-compat.js:4520-4527` | CharacterMutator.removeCharacter ✅ |

**结论**：✅ 唯一主入口已完全统一。

---

#### 域：`entities.quests`

| 入口 | 文件:行 | 路径 |
|------|---------|------|
| ✅ **唯一主入口** | `QuestMutator.setQuests` / `addQuest` | 4-291 |
| Mutator 不可用兜底（inline） | `ai-response-mutator.js:299-301` | ❌ 见 P1-8 |
| <mem> 标签处理 | `core.js:2847-2858` | QuestMutator.addQuest / setQuests ✅ |
| 剧情自动推进 | `ai-response-mutator.js:306-308` | QuestMutator.autoAdvanceByStory ✅ |
| 反向同步（gm→SM） | `core.js:271-273` `_syncQuestsToGameState` | StateManager.set 直写 ❌ 见 P1-1 |

**结论**：写入主入口已统一，但反向同步绕过 Mutator。

---

#### 域：`entities.locations`

| 入口 | 文件:行 | 路径 |
|------|---------|------|
| ✅ **唯一主入口** | `LocationMutator.setLocations` / `mergeLocations` / `addLocation` / `removeLocation` | 4-83 |
| AI 响应 | `ai-response-mutator.js:325-353` | LocationMutator.mergeLocations ✅ |
| **❌ UI 编辑** | `tavern-compat.js:4848-4893` | StateManager.set 直写 ❌ 见 **P0-2** |
| **❌ UI 新建** | `tavern-compat.js:4915-4935` | StateManager.set 直写 ❌ 见 **P0-2** |
| **❌ UI 删除** | `tavern-compat.js:4887-4895` | StateManager.set 直写 ❌ 见 **P0-2** |

**结论**：❌ MemoryManagerUI 的 locations 操作 3 处全部绕过 LocationMutator，是最严重的"未统一"领域。

---

#### 域：`entities.relationships`

| 入口 | 文件:行 | 路径 |
|------|---------|------|
| ✅ **唯一主入口** | `RelationshipMutator.mergeRelationships` / `clearRelationships` | 11-49 |
| AI 响应 | `ai-response-mutator.js:522-602` | RelationshipMutator.mergeRelationships + CharacterMutator.updateRelationship ✅ |
| 关系合并（legacy 调用） | `systems.js:845-879` | RelationshipMutator.mergeRelationships ✅ |
| 关系推断 | `systems.js:882-914` | AIResponseMutator._inferRelationshipsFromCharacters → mergeRelationships ✅ |
| **❌ 设定提取** | `core.js:5399-5414` | gm.tables 直写 + _syncRelationshipsToGameState ❌ 见 P2-3 |
| 反向同步（gm→SM） | `core.js:277-279` `_syncRelationshipsToGameState` | StateManager.set 直写 ❌ 见 P1-1 |

**结论**：❌ extractSetupToMemory 1 处 + 反向同步 1 处，2 个入口绕过 Mutator。

---

#### 域：`entities.events`

| 入口 | 文件:行 | 路径 |
|------|---------|------|
| AI 响应主入口 | `ai-response-mutator.js:451-507` | gm.addImportantEvents → SM.entities.events ✅ |
| <mem> 标签 | `core.js:2772-2785` | gm.addImportantEvent ✅ |
| 反向同步 | `core.js:283-294` `_syncEventsToKeyEvents` | StateManager.set 直写（events 数组） ⚠️ |

**结论**：写入收敛到 gm.addImportantEvents 唯一入口，反向同步略绕（但 events 是对象数组，无 Mutator）。

---

#### 域：`entities.currency`

| 入口 | 文件:行 | 路径 |
|------|---------|------|
| ✅ **唯一主入口** | `CurrencyMutator.set` / `add` / `spend` | 24-58 |
| AI 响应 | `ai-response-mutator.js:280-291` | StateManager.set 直写 ⚠️（无 Mutator 调用） |

**结论**：✅ 写入已统一到 CurrencyMutator。AI 响应直接 StateManager.set（无 normalize 链路但 OK，因为只有数值）。

---

#### 域：`entities.player`

| 入口 | 文件:行 | 路径 |
|------|---------|------|
| AI 响应 | `ai-response-mutator.js:198-243` | StateManager.set 直写 ⚠️（无 PlayerMutator） |
| 设定提取 | `core.js:5357-5358` | gm.addWorldAnchor（仅写 permanentFacts） ✅ |

**结论**：目前没有 PlayerMutator，AI 写入直接 StateManager.set 加上自己写 playerName。**未来应抽 PlayerMutator**。

---

#### 域：`time`

| 入口 | 文件:行 | 路径 |
|------|---------|------|
| ✅ **唯一主入口** | `TimeMutator.setTime` | 11-40 |
| AI 响应 | `core.js:2457-2510` GameTimeSystem.parseFromAI | TimeMutator.setTime ✅ |
| <mem> 标签 | `core.js:2867-2875` | TimeMutator.setTime ✅ |
| 撤销栈恢复 | `undo-mutator.js:71-123` | ❌ **不包含 time 字段** |

**结论**：✅ 写入统一。⚠️ UndoMutator 撤销时不恢复 time（snap 不存 time，可能是有意的）。

---

#### 域：`entities.relationships`（反向）

- 已有 `RelationshipMutator.mergeRelationships` 但反向同步（gm→SM）由 `_syncRelationshipsToGameState` 绕过 Mutator，详见 P1-1 + P2-3

---

### 5.3 关键数据流图

```
  ┌─────────────────────────────────────────┐
  │  Mutator 主路径                          │
  │  BagMutator / CharacterMutator / ...    │
  └─────────────┬───────────────────────────┘
                │ StateManager.set()
                ▼
  ┌─────────────────────────────────────────┐
  │  StateManager (唯一真相源)               │
  │  + _syncLegacyMirror → gameState.X      │
  └─────────────┬───────────────────────────┘
                │ 订阅 entities.** / progress.** / time
                ▼
  ┌─────────────────────────────────────────┐
  │  GameMemoryAdapter.syncToGameMemory     │
  │  - fieldMap (chars 漏 5 字段) ❌ P0-1   │
  │  - 缺失 syncFromGameMemory ❌ P1-1      │
  └─────────────┬───────────────────────────┘
                │
                ▼
  ┌─────────────────────────────────────────┐
  │  GameMemory (gm.tables / gm.quests /   │
  │  gm.events / gm.permanentFacts)         │
  └─────────────┬───────────────────────────┘
                │ 反向同步（gm→SM）
                │  _syncItemsToBag / _syncQuestsToGameState / _syncRelationshipsToGameState
                │  ⚠️ 全部用 StateManager.set 直写，绕过 Mutator
                ▼
  ┌─────────────────────────────────────────┐
  │  StateManager ← 重新接收                │
  └─────────────────────────────────────────┘

  ┌─────────────────────────────────────────┐
  │  ❌ 异常路径：MemoryManagerUI locations │
  │  → StateManager.set('entities.locations', ...) │
  │  绕过 LocationMutator ❌ P0-2            │
  └─────────────────────────────────────────┘
```

---

### 5.4 修复优先级建议

| 顺序 | 任务 | 估时 | 阻塞其他 |
|------|------|------|---------|
| 1 | P0-1 补全 characters fieldMap | 30 min | — |
| 2 | P0-2 MemoryManagerUI locations 改走 LocationMutator | 1 h | — |
| 3 | P0-3 清理 UndoMutator 注释 | 5 min | — |
| 4 | P1-6 _getRaw 危险键防护 | 10 min | — |
| 5 | P1-1 GameMemoryAdapter 加 syncFromGameMemory + 改写 core.js 反向同步 | 2 h | P2-3 |
| 6 | P1-2 BagMutator.mergeItems 字段白名单常量 | 30 min | — |
| 7 | P1-3 QuestMutator setQuests/addQuest 合并语义统一 | 1 h | — |
| 8 | P1-5 StateManager 加 peek | 30 min | — |
| 9 | P1-7 GameMemoryAdapter quests 同步补全字段 | 20 min | — |
| 10 | P1-8 AIResponseMutator fallback 改造为抛错 | 1 h | — |
| 11 | P2-1 8 Mutator API 规范化（按需补方法） | 2 h | — |
| 12 | P2-3 extractSetupToMemory 关系走 Mutator | 30 min | P1-1 |
| 13 | P2-5 schema undoHistory 重复键 | 10 min | — |
| 14 | P2-4 UndoMutator 补 setHistory/clearHistory | 20 min | — |
| 15 | P1-4 Mutator 补 normalize | 1 h | — |

**总计**：约 10-12 小时工作量

---

## 报告完结

- **审查人**：状态层架构审查子代理
- **报告路径**：`/workspace/docs/审查-state-2026-07-01.md`
- **后续**：本报告产出后，6-30 统筹版可作为历史归档，新一轮修复以本报告的 P0/P1 清单为基线
